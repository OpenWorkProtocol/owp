# Software Work Integrity vectors

Static portable-binding vectors. `expected_valid` is the expected result from `verifyBundle`. Real Git-object, ancestry, tree, diff, forbidden-path, clean-checkout, and command-execution attacks are covered dynamically by `tools/test-software-work-integrity.mjs`.
