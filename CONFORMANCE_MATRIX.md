# Normative requirement and conformance matrix

Revision: **1.0-rc3**
Generated: **2026-08-21** by `tools/build-requirement-matrix.mjs`

This inventory contains every RFC 2119 keyword occurrence in the normative
parts of the core specification, HTTP annex, and selected normative profiles. Sections 21–22 are historical and
informative and are deliberately excluded. One row is emitted per keyword,
including multiple obligations on one source line. IDs pin the RC3 candidate
file and line; regenerate and review the diff if normative text changes.

Counts: **MUST 133** · **MUST NOT 58** · **SHOULD 32** · **SHOULD NOT 0** · **MAY 33** · **total 256**.

`PASS` means the named evidence was reviewed in this release run. `N/A
(permission)` is the justified designation for a MAY. Conditional requirements
name the condition and do not count as exercised when that condition is absent.
The tests prove one reference implementation; they do not prove independent
interoperability.

| ID | Modality | Specification location | Requirement text | Applicable components | Automated or review evidence | Result | Remaining gap |
|---|---|---|---|---|---|---|---|
| OWP-L39-MUST-1 | MUST | [1. Purpose](spec/owp-1.0-rc3.md#L39) | A conforming deployment MUST preserve four properties. They are the protocol's purpose stated as tests: | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L44-MUST-NOT-1 | MUST NOT | [1. Purpose](spec/owp-1.0-rc3.md#L44) | **§1.1 — No agent blocks on a human.** Asking a question never stops work (§12). An agent that genuinely cannot proceed releases the work and takes other work; it MUST NOT idle holding a claim (§7.5). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L51-MUST-1 | MUST | [1. Purpose](spec/owp-1.0-rc3.md#L51) | **§1.3 — Human decisions should require minimal operator effort.** Every agent question MUST carry options and evidence sufficient to decide from the card alone (§12.1). The obligation is deliberately asymmetric: machine clients assemble the available evidence instead of shifting that work to the operator. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L81-MAY-1 | MAY | [2. Scope, and what OWP composes with](spec/owp-1.0-rc3.md#L81) | For software engineering work, OWP MAY compose with the normative optional **Software Work Integrity Profile** in [`annex-software-work-integrity-1.0-rc3.md`](spec/annex-software-work-integrity-1.0-rc3.md). That profile keeps Git, build systems, validators, attestations, and payment rails outside the surface while standardizing the portable evidence boundary: an agreed Definition of Done, an attempt-specific immutable source snapshot, a provider delivery claim, independent validation bound to the exact result, and validator-attested handoff. A core surface does not evaluate those objects; it round-trips them through ordinary envelopes and records the clients' acts. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L112-MUST-1 | MUST | [3. The line: envelopes and vocabularies](spec/owp-1.0-rc3.md#L112) | An **envelope** is a structure the surface stores, round-trips, and never interprets. Where the protocol needs open-endedness it MUST use an envelope rather than an enumeration: | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L130-MUST-NOT-1 | MUST NOT | [3. The line: envelopes and vocabularies](spec/owp-1.0-rc3.md#L130) | **An example may extend inside an envelope freely; it MUST NOT grow spec-shaped machinery of its own.** What cannot be expressed inside an envelope is protocol feedback, not deployment-specific protocol machinery. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L140-MUST-NOT-1 | MUST NOT | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L140) | **4.1 Frozen at 1.0.** The following MUST NOT change without a major revision: the four §1 properties; the surface-computes/clients-mutate-and-author invariant; the §3 envelope discipline; the verb names in §10 and their semantics; the state machine (§8); the fixed rank tiers (§11); the authority classes (§18); and the meaning of `record-only`. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| OWP-L148-MUST-1 | MUST | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L148) | **4.2 Additive without a revision bump.** New registered envelope values, new optional verbs, new optional fields, new observations, and new features declared through `surface.describe` (§9). A client MUST tolerate unknown fields and unknown envelope values (§19). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L151-SHOULD-1 | SHOULD | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L151) | A revision SHOULD publish a **machine-readable schema** alongside this prose (the reference implementation emits one from its single definition: `owp-code/schema/`). Where the two disagree, **the prose wins** — the schema is a convenience for generating types and validating payloads, not a second source of truth. Envelope *values* are deliberately absent from it: they belong to the deployment and are discovered through §9, not fixed in a file. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| OWP-L162-MAY-1 | MAY | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L162) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L163-MUST-1 | MUST | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L163) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L164-MUST-NOT-1 | MUST NOT | [4. Compatibility and stability](spec/owp-1.0-rc3.md#L164) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L179-MUST-NOT-1 | MUST NOT | [5. Roles](spec/owp-1.0-rc3.md#L179) | **Surface** — the server. Holds the working set, serves projections, assigns event order, computes derivations. It MUST NOT act on its own initiative. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L187-SHOULD-1 | SHOULD | [5. Roles](spec/owp-1.0-rc3.md#L187) | **Creator clients** — schedulers, watchers, janitors: ordinary clients that create proposals, unpark on triggers, release claims, or report observations, each per its own policy. **A creator client SHOULD own its own memory as well as its own clock**: idempotency ("did I already propose 16.0?") comes from a cursor the client keeps, never from querying the surface — a correct creator client needs zero read authority `[ops]`. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L196-MUST-NOT-1 | MUST NOT | [5. Roles](spec/owp-1.0-rc3.md#L196) | **Role routing is experience-side.** Deployments with several humans (an author and an editor; a reviewer and an approver) express "whose turn" in their operator client and in `park.trigger` prose. The protocol MUST NOT gain a human assignee field: it would import the human workflow OWP exists to avoid `[publish]`. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L228-MUST-1 | MUST | [7.1 Project](spec/owp-1.0-rc3.md#L228) | `key` prefixes every ref (`PAY-4`). **Refs are the only identifier anyone — human, agent, or UI — speaks, and MUST render identically everywhere.** `vocabulary` is the deployment's published envelope values (§9, §20). | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L256-MUST-1 | MUST | [7.2 Deliverable](spec/owp-1.0-rc3.md#L256) | **`state` above is the working set.** An item that leaves it (§8) keeps reporting a state, and reads MUST be able to say which: `completed` is §8's own exit, a proposal closed `record-only` (§12.3) reports `rejected`, and an explicitly withdrawn live item reports `cancelled` (§8). All three are terminal — no transition leaves them. They are defined by the protocol so every conforming client can parse all valid responses consistently. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L261-MUST-NOT-1 | MUST NOT | [7.2 Deliverable](spec/owp-1.0-rc3.md#L261) | `parent` is delegation only; depth MUST NOT exceed 2. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L279-MUST-1 | MUST | [7.3 Links](spec/owp-1.0-rc3.md#L279) | The envelope is normative; the vocabulary is not. Surfaces **MUST preserve (round-trip) link types they do not understand.** `references` is the one registered type, because promotion and provenance depend on it (§12.4). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L289-SHOULD-1 | SHOULD | [7.3 Links](spec/owp-1.0-rc3.md#L289) | **Write semantics (normative).** `work.update(ref, {links})` **replaces each named type's entry array wholesale**; types not named are preserved untouched. A client patching a multi-entry type therefore supplies the complete set — which a creator client holds in its own memory (§5), never by reading the surface. Per-entry merge would require the surface to know an entry's identity key, which is vocabulary and therefore out; deployments whose entries have independent writers SHOULD model finer-grained deliverables instead `[ops]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L299-MUST-NOT-1 | MUST NOT | [7.4 Event](spec/owp-1.0-rc3.md#L299) | `seq` is a surface-assigned **total order**. Clients MUST NOT order by timestamp. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L301-MAY-1 | MAY | [7.4 Event](spec/owp-1.0-rc3.md#L301) | Reads are cursor-based (`since: seq`) and MAY be bounded by `limit`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L302-MUST-1 | MUST | [7.4 Event](spec/owp-1.0-rc3.md#L302) | Reads are cursor-based (`since: seq`) and MAY be bounded by `limit`. - **`actor`** is a registered session id **or** a client identity. Clients MUST be able to distinguish the two; a session id resolves through `session.view` (§15) and a client identity does not. *(`session` is emitted as a deprecated alias for one revision — §4.5.)* | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L306-MUST-NOT-1 | MUST NOT | [7.4 Event](spec/owp-1.0-rc3.md#L306) | **The surface MUST NOT author events whose body is prose.** State transitions are recorded as state. A surface appends exactly three kinds on its own: `unparked` (§7.5, body is data), `completed` (§13, body is the record), and `cancelled` (§8, body is the client's required reason). Narration is a client's business. *An editorial desk, where notes are the product, cannot distinguish machine narration from an editor's note* `[publish]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L327-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc3.md#L327) | 1. **Parking releases ownership.** `owner_session` clears. 2. **Parking MUST include the handoff payload** — all four fields. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L330-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc3.md#L330) | 4. `question` names the gating question for `cause: decision`; if present, only a decision on *that* question releases the park. **The question MUST be open on this item** — a surface rejects a park naming one that is not (`STATE`), and a decision releases parks on its own item only (§12.2). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L338-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc3.md#L338) | 6. The release MUST emit an `unparked` event carrying the park payload, so the handoff survives into the resuming session's assignment packet. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L365-MUST-1 | MUST | [7.7 CompletionRecord](spec/owp-1.0-rc3.md#L365) | `outcome` MUST be present; the rest default to empty. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L384-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L384) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L385-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L385) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L385-MUST-2 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L385) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L387-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L387) | A surface MUST refuse a non-completed exit (`cancelled` or `rejected`) with `STATE` while any non-exited item directly depends on the target. The client cancels or rewires downstream work first; silently stranding it would violate the chooser's dependency contract. For the same reason, `work.create` and `work.update` MUST refuse `depends_on` entries already `cancelled` or `rejected`; only a live target or `completed` exit can ever satisfy the edge. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L391-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L391) | A surface MUST refuse a non-completed exit (`cancelled` or `rejected`) with `STATE` while any non-exited item directly depends on the target. The client cancels or rewires downstream work first; silently stranding it would violate the chooser's dependency contract. For the same reason, `work.create` and `work.update` MUST refuse `depends_on` entries already `cancelled` or `rejected`; only a live target or `completed` exit can ever satisfy the edge. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L393-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L393) | Cancellation MUST clear ownership, park and pending completion data. A holder's `current_item` MUST also clear. A second cancellation is `STATE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L394-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L394) | Cancellation MUST clear ownership, park and pending completion data. A holder's `current_item` MUST also clear. A second cancellation is `STATE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L395-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc3.md#L395) | Cancellation is not deletion. `work.view` MUST continue to return the record, reasoned event, and terminal state. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L409-MUST-NOT-1 | MUST NOT | [8. State and observations](spec/owp-1.0-rc3.md#L409) | `unreconciled` is envelope-shaped: a **watcher client** reports what it observed into any link entry; the surface derives the observation from data it already holds; a session clears it by writing the worklog forward. **The surface MUST NOT read the outside world** `[ops]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L414-MUST-NOT-1 | MUST NOT | [8. State and observations](spec/owp-1.0-rc3.md#L414) | **The surface never acts on an observation.** There is no expiry, no auto-release, and no protocol-level threshold — what reads as "stalled" is deployment configuration. Recovery of work held by a dead session is a **client** action. Elapsed time exists only as reported observations and MUST NOT schedule or mutate anything. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L423-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L423) | **`surface.describe()`** is the one bootstrap read. It MUST be callable without a session. A credentialed binding MAY require a valid credential; if it does, missing or invalid credentials answer `UNAUTHENTICATED` like any other verb. It returns an envelope whose slots the spec names and whose values the deployment fills: | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L424-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc3.md#L424) | **`surface.describe()`** is the one bootstrap read. It MUST be callable without a session. A credentialed binding MAY require a valid credential; if it does, missing or invalid credentials answer `UNAUTHENTICATED` like any other verb. It returns an envelope whose slots the spec names and whose values the deployment fills: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L444-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L444) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L444-MUST-2 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L444) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L446-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L446) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L449-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L449) | A surface MUST report an authenticated or open-mode caller's **own** authority class and grants. A client learning its authority by receiving a `FORBIDDEN` is the failure this removes. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L453-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L453) | A surface MUST list the verbs it implements, so optional features are discoverable rather than probed. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L455-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc3.md#L455) | A conforming deployment MUST publish its vocabulary here (§20). This is §3's obligation made machine-readable. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L457-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc3.md#L457) | A surface with many projects MAY report only those the caller may act on, and MAY omit `projects` entirely for an unauthenticated caller. Discovery is a bootstrap read, not a directory: a client that needs the full portfolio calls `portfolio` (§15), which is scoped and bounded. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L458-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc3.md#L458) | A surface with many projects MAY report only those the caller may act on, and MAY omit `projects` entirely for an unauthenticated caller. Discovery is a bootstrap read, not a directory: a client that needs the full portfolio calls `portfolio` (§15), which is scoped and bounded. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L473-SHOULD-1 | SHOULD | [Agent-side (require a registered session)](spec/owp-1.0-rc3.md#L473) | | `session.heartbeat()` | Lightweight liveness; SHOULD piggyback on every verb | | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L474-MUST-NOT-1 | MUST NOT | [Agent-side (require a registered session)](spec/owp-1.0-rc3.md#L474) | | `session.end()` | Marks the session ended. MUST NOT release its claims — that is a client decision (§8) | | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L478-MUST-NOT-1 | MUST NOT | [Agent-side (require a registered session)](spec/owp-1.0-rc3.md#L478) | | `work.create(item, dispatch)` → ref | `dispatch:true` ⇒ todo; `false` ⇒ `proposed`. Dispatch MUST NOT cross projects | | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L484-MUST-1 | MUST | [Agent-side (require a registered session)](spec/owp-1.0-rc3.md#L484) | | `policy.applicable(ref)` → policies | All active policies in scope; MUST be consulted before irreversible actions | | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L516-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L516) | `work.next` is the most-called verb and MUST be deterministic enough that two conforming surfaces hand the same session the same work. **`work.next` chooses AND claims atomically**; a choose-then-claim split races two sessions onto one item. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L532-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L532) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L532-MUST-2 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L532) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L533-MUST-NOT-1 | MUST NOT | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L533) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L551-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L551) | **The ordinal MUST be observable**, or §20 cannot check the tier that carries §11's interoperability promise. It already is: refs are `KEY-N` with `N` assigned in creation order within a project (§7.1), and the chooser only ever ranks within one project — so `N` *is* the ordinal, and any client can verify the ordering from the refs it was handed. A surface MUST NOT use a different ordinal than the one its refs expose. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L555-MUST-NOT-1 | MUST NOT | [11. Assignment — the work.next chooser](spec/owp-1.0-rc3.md#L555) | **The ordinal MUST be observable**, or §20 cannot check the tier that carries §11's interoperability promise. It already is: refs are `KEY-N` with `N` assigned in creation order within a project (§7.1), and the chooser only ever ranks within one project — so `N` *is* the ordinal, and any client can verify the ordering from the refs it was handed. A surface MUST NOT use a different ordinal than the one its refs expose. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L562-MUST-1 | MUST | [12.1 Questions](spec/owp-1.0-rc3.md#L562) | An agent `question` body MUST carry `options[]`, and **every option MUST carry `evidence[]`** — entries are envelopes (§3) and round-trip: | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L562-MUST-2 | MUST | [12.1 Questions](spec/owp-1.0-rc3.md#L562) | An agent `question` body MUST carry `options[]`, and **every option MUST carry `evidence[]`** — entries are envelopes (§3) and round-trip: | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L580-MAY-1 | MAY | [12.1 Questions](spec/owp-1.0-rc3.md#L580) | **A question body MAY carry `direction`** (`to_operator` | `to_session`). When it is absent a surface infers the old default — a registered session's question runs downstream to the operator, anyone else's runs upstream (§12.6) — so nothing already written changes meaning. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L590-MUST-1 | MUST | [12.1 Questions](spec/owp-1.0-rc3.md#L590) | The author's registration status previously controlled two independent concerns: the evidence obligation and the routing direction. A creator client demonstrates why those concerns must remain separate `[publish]`: a machine watcher should still supply options and evidence under §12.1 while being able to direct its question to a human operator. **The obligation stays attached to the author's class; only the routing moved.** A surface MUST honour a stated `direction` in both places the routing appears: the attention queue (§15) and the assignment packet (§10). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L599-MUST-1 | MUST | [12.2 Decisions](spec/owp-1.0-rc3.md#L599) | **A decision is item-local.** The question gating a decision-park MUST be open on the parked item itself (§7.5 rule 4), and answering it releases parks on **that item only** — never every park elsewhere that happens to name the same seq. Both ends were unstated, and two conforming surfaces could read the same sentences opposite ways, with no test to tell them apart `[publish]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L615-MUST-1 | MUST | [12.3 Continuations](spec/owp-1.0-rc3.md#L615) | **Every rejection MUST name its continuation.** The vocabulary is an envelope with exactly one registered value: | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L625-MAY-1 | MAY | [12.4 Promotion](spec/owp-1.0-rc3.md#L625) | **One mechanism, two targets.** Any decision artifact — an option, an answer, a rejection, an editorial note — MAY be promoted into a **proposal** or a **policy**, with `provenance` naming the originating event and item. The provenance chain is the point: it is how runbooks, style guides, and tried-and-rejected lists write themselves `[publish][ops]`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L632-MAY-1 | MAY | [12.5 The operator's voice](spec/owp-1.0-rc3.md#L632) | An operator or creator client MAY append `note` events to any item. A note is **context without a verdict**: nothing changes state, and the note rides the worklog into whichever session works the item next. Client actors are restricted to `note` and `question`; answers go through `answer`, verdicts through `reject`/`triage`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L640-MAY-1 | MAY | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc3.md#L640) | An operator **or a creator client** MAY author a `question` on an item. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L647-SHOULD-1 | SHOULD | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc3.md#L647) | The evidence obligation of §12.1 does **not** apply *to a human*: requiring a human to assemble options would violate §1.3 in the direction the protocol protects. It is not an exemption for everything that is not a session. A creator client is a machine, and one that asks the operator SHOULD carry options and evidence exactly as a session does. A surface cannot tell the two apart — client identity is not a claim about being human — so this is an obligation on authors, not a schema a surface can enforce. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| OWP-L658-MUST-1 | MUST | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc3.md#L658) | **An operator client MUST be able to tell the two directions apart**, and a surface MUST let it: `work.view`'s `open_questions` entries carry `direction`. Routing them correctly into the queue while leaving the item read ambiguous is not enough — a deployment rendered its own operator's question back to that operator as a fork to decide, and since §12.1 excuses humans from assembling options, it arrived as a decision with nothing to decide between `[research]`. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| OWP-L659-MUST-1 | MUST | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc3.md#L659) | **An operator client MUST be able to tell the two directions apart**, and a surface MUST let it: `work.view`'s `open_questions` entries carry `direction`. Routing them correctly into the queue while leaving the item read ambiguous is not enough — a deployment rendered its own operator's question back to that operator as a fork to decide, and since §12.1 excuses humans from assembling options, it arrived as a decision with nothing to decide between `[research]`. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| OWP-L673-MUST-1 | MUST | [13.1 Completion](spec/owp-1.0-rc3.md#L673) | `work.complete` MUST include a valid CompletionRecord. Two planes meet here, and this is the point where the protocol requires a concise structured record. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L689-MAY-1 | MAY | [13.1 Completion](spec/owp-1.0-rc3.md#L689) | 3. A client with appropriate authority MAY complete straight to exit where the deployment's policy allows it. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L703-MAY-1 | MAY | [13.2 The knowledge plane](spec/owp-1.0-rc3.md#L703) | The protocol defines this **contract** and not a retrieval mechanism. `knowledge.query` is deliberately thin; a surface declares its retrieval capability through `surface.describe().features.knowledge_query` and MAY answer `none`. Retrieval strategy—grep, full-text search, embeddings, or another mechanism—is a deployment concern. Specifying it here would turn the protocol into a search-engine contract. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L766-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L766) | **`policies` carries the standing rules for an act that cannot be undone.** A row MUST carry the applicable policies (§7.6, same set and order as `policy.applicable`) when the action that clears it is irreversible, and the field is absent when none apply. §10 makes an agent consult policy before an irreversible action and hands it the applicable rules in its assignment packet; the operator's own projections did not carry the same rules. For an irreversible operator decision, that made the row incomplete under §1.3 even though the agent assignment contained the necessary policy context `[ops]`. `triage`, `answer` and `unpark` are irreversible; `release` is not (the item returns to `todo` and keeps its worklog), and `reconcile` is not the reader's act at all. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L778-MUST-NOT-1 | MUST NOT | [15. Projections](spec/owp-1.0-rc3.md#L778) | **`action` names the act that clears the row — not necessarily an act its reader may perform.** A row whose clearing act belongs to another actor is **informational**: a client MUST NOT offer it as an operator action and SHOULD render it as an observation. `reconcile` is the one such action this document defines — §8 says a session clears drift by writing the worklog forward, so nothing an operator client offers will clear that row. A reference client previously mapped such a row to `release`, which removed a claim held by an active agent `[code]`. Clients branch on `action`; `reason` is prose for a person and never a contract. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L779-SHOULD-1 | SHOULD | [15. Projections](spec/owp-1.0-rc3.md#L779) | **`action` names the act that clears the row — not necessarily an act its reader may perform.** A row whose clearing act belongs to another actor is **informational**: a client MUST NOT offer it as an operator action and SHOULD render it as an observation. `reconcile` is the one such action this document defines — §8 says a session clears drift by writing the worklog forward, so nothing an operator client offers will clear that row. A reference client previously mapped such a row to `release`, which removed a claim held by an active agent `[code]`. Clients branch on `action`; `reason` is prose for a person and never a contract. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L785-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L785) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L788-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L788) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L789-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L789) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L799-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L799) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L801-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc3.md#L801) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L801-MUST-NOT-2 | MUST NOT | [15. Projections](spec/owp-1.0-rc3.md#L801) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L851-MUST-1 | MUST | [16. Errors](spec/owp-1.0-rc3.md#L851) | A surface MUST answer failures with these codes. Clients branch on them, so they are protocol, not binding detail. Transport mapping belongs to the binding annex (§17). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L862-MUST-NOT-1 | MUST NOT | [16. Errors](spec/owp-1.0-rc3.md#L862) | | `FORBIDDEN` | authority, grant, or project-boundary violation | **MUST NOT retry**; audited (§18) | | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L867-MAY-1 | MAY | [16. Errors](spec/owp-1.0-rc3.md#L867) | A surface MAY add codes; a client MUST treat an unrecognized code as terminal. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L867-MUST-2 | MUST | [16. Errors](spec/owp-1.0-rc3.md#L867) | A surface MAY add codes; a client MUST treat an unrecognized code as terminal. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L871-MUST-NOT-1 | MUST NOT | [16. Errors](spec/owp-1.0-rc3.md#L871) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L874-MUST-1 | MUST | [16. Errors](spec/owp-1.0-rc3.md#L874) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L876-SHOULD-1 | SHOULD | [16. Errors](spec/owp-1.0-rc3.md#L876) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| OWP-L876-MUST-2 | MUST | [16. Errors](spec/owp-1.0-rc3.md#L876) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| OWP-L879-SHOULD-1 | SHOULD | [16. Errors](spec/owp-1.0-rc3.md#L879) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L923-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L923) | **Agent authority**: sessions, work, events, own-claim release, discovery. MUST NOT triage, promote, set or retire policy, cancel work, unpark others' triggers, release others' claims, or delete. **`work.release` is agent authority only for the caller's own claim**; releasing another session's claim requires operator authority or a grant — which is what makes the janitor grant (`{verbs:["work.release"]}`) mean something rather than restate a power the class already had `[code]`. This binds the **class**, not the session: an agent-authority client that registered no session holds no claim of its own, so it needs the grant like anyone else. A surface that tests "is this a different session?" instead of "does this actor own this claim?" can allow a credential that omits the session header to release a claim it does not own; the reference implementation previously exhibited this behavior `[code]`. An agent MAY resolve session identities **within its own project** (§15) — §7.4 requires every client to tell a session id from a client identity, and a session already sees peer ids in `owner_session`, so the resolution leaks nothing and the alternative is a requirement no agent can obey. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L934-MAY-1 | MAY | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L934) | **Agent authority**: sessions, work, events, own-claim release, discovery. MUST NOT triage, promote, set or retire policy, cancel work, unpark others' triggers, release others' claims, or delete. **`work.release` is agent authority only for the caller's own claim**; releasing another session's claim requires operator authority or a grant — which is what makes the janitor grant (`{verbs:["work.release"]}`) mean something rather than restate a power the class already had `[code]`. This binds the **class**, not the session: an agent-authority client that registered no session holds no claim of its own, so it needs the grant like anyone else. A surface that tests "is this a different session?" instead of "does this actor own this claim?" can allow a credential that omits the session header to release a claim it does not own; the reference implementation previously exhibited this behavior `[code]`. An agent MAY resolve session identities **within its own project** (§15) — §7.4 requires every client to tell a session id from a client identity, and a session already sees peer ids in `owner_session`, so the resolution leaks nothing and the alternative is a requirement no agent can obey. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L956-MUST-1 | MUST | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L956) | **Grants compose: union across grants, intersection within one.** A client holding `{work.unpark, "window:"}` and `{work.unpark, "lock:"}` owns both prefixes. A surface that resolves only the first grant naming a verb silently voids the rest. Within a single grant, **every field a grant names MUST hold** on every path by which the verb can be reached. A surface that checked `trigger_prefix` but ignored `project` broadened both the janitor and cron grants beyond their intended scope `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L962-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L962) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L963-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L963) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L964-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L964) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L978-MUST-1 | MUST | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L978) | **Authorship is recorded, never inferred.** A surface enforcing this MUST know who authored the completion record and who proposed the item. Deriving it from event history is guesswork that fails on exactly the items with complicated histories `[desk][research]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L983-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L983) | A surface that cannot determine authorship for a given item MUST NOT substitute a guess: the invariant does not apply when the required authorship fact was not recorded. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L986-SHOULD-1 | SHOULD | [18. Security — authority, authentication, audit](spec/owp-1.0-rc3.md#L986) | **Audit.** Surfaces SHOULD log every verb call with its actor, **including denials**. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1000-MAY-1 | MAY | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1000) | **All other names are the deployment's.** Two deployments MAY use the same name for different shapes; they do not interoperate on that name, which is the expected consequence of local vocabulary. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L1003-MUST-1 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1003) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1004-MUST-NOT-1 | MUST NOT | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1004) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L1004-MUST-2 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1004) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L1009-MUST-1 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1009) | **No-calendar reserved names.** The field names `due`, `deadline`, `due_date`, and `expected_by` are reserved so the §1 no-calendar invariant cannot be reintroduced accidentally as a deployment extension. When one is supplied on a protocol record, a surface MUST either reject the record or ignore the field. It MUST NOT store, return, rank, transition, schedule, or otherwise interpret the value as protocol semantics. This rule is about fields, not open envelope values: deployments remain free to use temporal words inside their own links, triggers, policies, and prose. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1010-MUST-NOT-1 | MUST NOT | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1010) | **No-calendar reserved names.** The field names `due`, `deadline`, `due_date`, and `expected_by` are reserved so the §1 no-calendar invariant cannot be reintroduced accidentally as a deployment extension. When one is supplied on a protocol record, a surface MUST either reject the record or ignore the field. It MUST NOT store, return, rank, transition, schedule, or otherwise interpret the value as protocol semantics. This rule is about fields, not open envelope values: deployments remain free to use temporal words inside their own links, triggers, policies, and prose. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1015-SHOULD-1 | SHOULD | [19. Extensions and registered names](spec/owp-1.0-rc3.md#L1015) | A deployment publishes its vocabulary through `surface.describe` (§9) and SHOULD document it in prose beside its code. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L1038-MUST-1 | MUST | [20. Conformance](spec/owp-1.0-rc3.md#L1038) | **A conforming deployment MUST publish its vocabulary** through §9. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L1042-MUST-NOT-1 | MUST NOT | [20. Conformance](spec/owp-1.0-rc3.md#L1042) | A deployment that declares the Software Work Integrity Profile is additionally conformant to that profile only when its participating clients and independent validator satisfy Annex B. Core OWP conformance alone MUST NOT be represented as proof of exact-source or build provenance. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| HTTP-L24-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc3.md#L24) | Every verb request MUST carry `Content-Type: application/json` (an optional charset parameter is allowed). A surface MUST reject any other or absent media type with `VALIDATION`; accepting form-compatible types lets an unrelated browser origin drive a loopback surface with ambient authority. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L25-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc3.md#L25) | Every verb request MUST carry `Content-Type: application/json` (an optional charset parameter is allowed). A surface MUST reject any other or absent media type with `VALIDATION`; accepting form-compatible types lets an unrelated browser origin drive a loopback surface with ambient authority. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L28-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc3.md#L28) | A surface MUST reject a verb body larger than 1 MiB with `VALIDATION`, before parsing or dispatch. Deployments MAY impose a smaller documented limit. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L29-MAY-1 | MAY | [A.1 Routes](spec/annex-http-1.0-rc3.md#L29) | A surface MUST reject a verb body larger than 1 MiB with `VALIDATION`, before parsing or dispatch. Deployments MAY impose a smaller documented limit. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L31-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc3.md#L31) | A path that is verb-shaped but names a verb the surface does not implement MUST answer `UNKNOWN_VERB` — **not** a transport 404. A binding that conflates "this route did not parse" with "this surface lacks that verb" makes discovery unusable, and clients cannot tell a missing feature from a typo'd URL. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L35-MAY-1 | MAY | [A.1 Routes](spec/annex-http-1.0-rc3.md#L35) | Surfaces MAY serve other paths (an operator UI at `/`, static assets); those are outside this annex. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L46-MAY-1 | MAY | [A.2 Actor identity](spec/annex-http-1.0-rc3.md#L46) | A request MAY carry both a session and a client identity; the session wins for attribution (§7.4). Every verb call refreshes the session's `last_seen`, so heartbeats piggyback. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L57-MAY-1 | MAY | [A.3 Responses](spec/annex-http-1.0-rc3.md#L57) | `result` MAY be `null` — notably `work.next` with nothing eligible, which is a normal answer and MUST NOT be an error. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L58-MUST-NOT-1 | MUST NOT | [A.3 Responses](spec/annex-http-1.0-rc3.md#L58) | `result` MAY be `null` — notably `work.next` with nothing eligible, which is a normal answer and MUST NOT be an error. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L77-MUST-1 | MUST | [A.4 Error mapping](spec/annex-http-1.0-rc3.md#L77) | Clients MUST branch on `error.code`, never on the status alone. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L81-MUST-NOT-1 | MUST NOT | [A.4 Error mapping](spec/annex-http-1.0-rc3.md#L81) | **A failure to reach the surface has no code in this table and no status at all** (§16). A connection refused, a DNS failure, or a timeout is not an answer, and a client MUST NOT map it onto one of the codes above — `UNKNOWN_VERB` for a 404 from a load balancer, or `VALIDATION` for a 400 from a proxy, turns a retryable outage into a terminal error. A proxying operator client that must report the failure in the §16 envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`, and a 502 or 504 from an intermediary belongs in the same class. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L84-SHOULD-1 | SHOULD | [A.4 Error mapping](spec/annex-http-1.0-rc3.md#L84) | **A failure to reach the surface has no code in this table and no status at all** (§16). A connection refused, a DNS failure, or a timeout is not an answer, and a client MUST NOT map it onto one of the codes above — `UNKNOWN_VERB` for a 404 from a load balancer, or `VALIDATION` for a 400 from a proxy, turns a retryable outage into a terminal error. A proxying operator client that must report the failure in the §16 envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`, and a 502 or 504 from an intermediary belongs in the same class. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L90-MAY-1 | MAY | [A.5 Revision selection](spec/annex-http-1.0-rc3.md#L90) | A client MAY pin a revision with `x-owp-protocol: 1.0-rc3`. A surface that does not implement the pinned revision MUST answer `UNSUPPORTED_VERSION` with `data.supported`. Absent the header, the surface answers in its default revision, which `surface.describe` reports. Discovery (§9) is the intended handshake: call `surface.describe` first, then pin. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L91-MUST-1 | MUST | [A.5 Revision selection](spec/annex-http-1.0-rc3.md#L91) | A client MAY pin a revision with `x-owp-protocol: 1.0-rc3`. A surface that does not implement the pinned revision MUST answer `UNSUPPORTED_VERSION` with `data.supported`. Absent the header, the surface answers in its default revision, which `surface.describe` reports. Discovery (§9) is the intended handshake: call `surface.describe` first, then pin. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L98-MAY-1 | MAY | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L98) | A client MAY send `Idempotency-Key` on any verb request. The value MUST be 1–128 ASCII characters from `A-Z`, `a-z`, `0-9`, `.`, `_`, `:`, or `-`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L98-MUST-2 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L98) | A client MAY send `Idempotency-Key` on any verb request. The value MUST be 1–128 ASCII characters from `A-Z`, `a-z`, `0-9`, `.`, `_`, `:`, or `-`. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L101-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L101) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L102-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L102) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L103-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L103) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L104-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L104) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L106-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L106) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L107-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L107) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L108-MUST-NOT-1 | MUST NOT | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L108) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L110-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L110) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L111-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L111) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L112-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L112) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L112-MUST-2 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L112) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L114-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L114) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L115-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc3.md#L115) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L121-MAY-1 | MAY | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L121) | Authentication is a binding concern (§18); this annex describes the reference arrangement, which a deployment MAY replace wholesale: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L128-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L128) | **Open mode** — no credentials configured. Authority is inferred: session actors are agent-class, bare clients operator-class. Appropriate only where the transport itself is the trust boundary: one omitted header buys operator authority, so the reference surface binds **loopback only** in open mode and exposing it on a routable interface is an explicit opt-out. Loopback alone is not a browser security boundary: implementations MUST also enforce A.1's JSON-only request rule and SHOULD reject unexpected `Origin` headers. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L129-SHOULD-1 | SHOULD | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L129) | **Open mode** — no credentials configured. Authority is inferred: session actors are agent-class, bare clients operator-class. Appropriate only where the transport itself is the trust boundary: one omitted header buys operator authority, so the reference surface binds **loopback only** in open mode and exposing it on a routable interface is an explicit opt-out. Loopback alone is not a browser security boundary: implementations MUST also enforce A.1's JSON-only request rule and SHOULD reject unexpected `Origin` headers. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L136-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L136) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L137-MUST-NOT-1 | MUST NOT | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L137) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L137-SHOULD-2 | SHOULD | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L137) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L140-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc3.md#L140) | `surface.describe` MUST report the caller's own authority and grants, so a client never has to learn its permissions by being refused. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L146-SHOULD-1 | SHOULD | [A.8 Deprecations carried in 1.0](spec/annex-http-1.0-rc3.md#L146) | Per §4.5, event payloads carry both `actor` (normative) and `session` (the deprecated alias). Clients SHOULD read `actor`; surfaces SHOULD emit both until a revision removes the alias. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| HTTP-L146-SHOULD-2 | SHOULD | [A.8 Deprecations carried in 1.0](spec/annex-http-1.0-rc3.md#L146) | Per §4.5, event payloads carry both `actor` (normative) and `session` (the deprecated alias). Clients SHOULD read `actor`; surfaces SHOULD emit both until a revision removes the alias. | Specification and schema maintainers | Published RC2 reference evidence plus RC3 prose/compatibility diff review; RELEASE_READINESS.md records the external RC3 implementation gate. | PASS | None. |
| SWI-L39-MAY-1 | MAY | [B.1 Profile boundary and actors](spec/annex-software-work-integrity-1.0-rc3.md#L39) | The provider and orchestrator MAY be operated by the same organization. The OWP validator MUST be controlled independently of both for a flow that claims `independent_validation: true`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L40-MUST-1 | MUST | [B.1 Profile boundary and actors](spec/annex-software-work-integrity-1.0-rc3.md#L40) | The provider and orchestrator MAY be operated by the same organization. The OWP validator MUST be controlled independently of both for a flow that claims `independent_validation: true`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L45-MUST-1 | MUST | [B.1 Profile boundary and actors](spec/annex-software-work-integrity-1.0-rc3.md#L45) | The protocol never assumes an execution worker is trustworthy. A provider, orchestrator, subagent, CI job controlled by them, or worker-produced manifest MUST be treated as a **claim source**, not as validation authority. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L47-MUST-NOT-1 | MUST NOT | [B.1 Profile boundary and actors](spec/annex-software-work-integrity-1.0-rc3.md#L47) | A deployment MUST NOT call a delivery independently validated merely because a provider signed it, a CI system under provider control reported success, a payment settled, or the event ledger preserved the claim without modification. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L55-SHOULD-1 | SHOULD | [B.2 Registered profile link types](spec/annex-software-work-integrity-1.0-rc3.md#L55) | A deployment selecting this profile SHOULD publish these globally scoped link types in its project vocabulary: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L64-SHOULD-1 | SHOULD | [B.2 Registered profile link types](spec/annex-software-work-integrity-1.0-rc3.md#L64) | A link entry SHOULD contain an immutable artifact URI when one exists and MUST contain the digest required by the relevant section below. The OWP surface MUST round-trip these entries exactly and MUST NOT interpret their Git, test, payment, or attestation semantics. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L64-MUST-2 | MUST | [B.2 Registered profile link types](spec/annex-software-work-integrity-1.0-rc3.md#L64) | A link entry SHOULD contain an immutable artifact URI when one exists and MUST contain the digest required by the relevant section below. The OWP surface MUST round-trip these entries exactly and MUST NOT interpret their Git, test, payment, or attestation semantics. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L65-MUST-1 | MUST | [B.2 Registered profile link types](spec/annex-software-work-integrity-1.0-rc3.md#L65) | A link entry SHOULD contain an immutable artifact URI when one exists and MUST contain the digest required by the relevant section below. The OWP surface MUST round-trip these entries exactly and MUST NOT interpret their Git, test, payment, or attestation semantics. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L66-MUST-NOT-1 | MUST NOT | [B.2 Registered profile link types](spec/annex-software-work-integrity-1.0-rc3.md#L66) | A link entry SHOULD contain an immutable artifact URI when one exists and MUST contain the digest required by the relevant section below. The OWP surface MUST round-trip these entries exactly and MUST NOT interpret their Git, test, payment, or attestation semantics. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L83-MUST-1 | MUST | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L83) | MUST use printable ASCII object/property names; | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L84-MUST-NOT-1 | MUST NOT | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L84) | MUST use printable ASCII object/property names; - MUST NOT contain JSON floating-point numbers; | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L85-MUST-1 | MUST | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L85) | MUST NOT contain JSON floating-point numbers; - MUST use decimal strings for monetary values; | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L86-MUST-1 | MUST | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L86) | MUST use decimal strings for monetary values; - MUST use integers only inside the interoperable IEEE-754 safe-integer range; | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L87-MUST-1 | MUST | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L87) | MUST use integers only inside the interoperable IEEE-754 safe-integer range; - MUST preserve Unicode string values exactly as JCS requires. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L89-MUST-1 | MUST | [B.3 Canonical profile digests](spec/annex-software-work-integrity-1.0-rc3.md#L89) | An implementation MUST reject a claimed digest whose canonical payload does not recompute to the claim. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L99-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L99) | Before the first paid or otherwise authorized execution Attempt, the parties MUST agree on one immutable `contract_body` containing at least: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L127-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L127) | Each Definition-of-Done item MUST have a stable `id`, a tier, and an objective statement. Deterministic items MUST additionally describe an executable command or an equivalently precise machine policy. Evidence-backed items MUST describe the evidence and integrity check. Human-judged items MUST be explicitly marked `human` and MUST NOT be misrepresented as machine-validated. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L128-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L128) | Each Definition-of-Done item MUST have a stable `id`, a tier, and an objective statement. Deterministic items MUST additionally describe an executable command or an equivalently precise machine policy. Evidence-backed items MUST describe the evidence and integrity check. Human-judged items MUST be explicitly marked `human` and MUST NOT be misrepresented as machine-validated. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L129-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L129) | Each Definition-of-Done item MUST have a stable `id`, a tier, and an objective statement. Deterministic items MUST additionally describe an executable command or an equivalently precise machine policy. Evidence-backed items MUST describe the evidence and integrity check. Human-judged items MUST be explicitly marked `human` and MUST NOT be misrepresented as machine-validated. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L130-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L130) | Each Definition-of-Done item MUST have a stable `id`, a tier, and an objective statement. Deterministic items MUST additionally describe an executable command or an equivalently precise machine policy. Evidence-backed items MUST describe the evidence and integrity check. Human-judged items MUST be explicitly marked `human` and MUST NOT be misrepresented as machine-validated. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L131-MUST-NOT-1 | MUST NOT | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L131) | Each Definition-of-Done item MUST have a stable `id`, a tier, and an objective statement. Deterministic items MUST additionally describe an executable command or an equivalently precise machine policy. Evidence-backed items MUST describe the evidence and integrity check. Human-judged items MUST be explicitly marked `human` and MUST NOT be misrepresented as machine-validated. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L133-MUST-1 | MUST | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L133) | The contract body MUST distinguish objective delivery validity from final customer preference. A validator's `VALID` result MUST NOT be treated as a customer `APPROVE` decision. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L134-MUST-NOT-1 | MUST NOT | [B.4.1 Contract body](spec/annex-software-work-integrity-1.0-rc3.md#L134) | The contract body MUST distinguish objective delivery validity from final customer preference. A validator's `VALID` result MUST NOT be treated as a customer `APPROVE` decision. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L158-MUST-1 | MUST | [B.4.2 Contract digest and acknowledgements](spec/annex-software-work-integrity-1.0-rc3.md#L158) | The customer MUST acknowledge that the Definition of Done expresses the desired outcome and customer-owned human criteria. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L161-MUST-1 | MUST | [B.4.2 Contract digest and acknowledgements](spec/annex-software-work-integrity-1.0-rc3.md#L161) | The provider MUST acknowledge that it is willing to deliver against that exact Definition of Done. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L164-MUST-1 | MUST | [B.4.2 Contract digest and acknowledgements](spec/annex-software-work-integrity-1.0-rc3.md#L164) | The orchestrator MUST acknowledge that it can provision and supervise an execution capable of satisfying the required evidence policy. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L167-MUST-1 | MUST | [B.4.2 Contract digest and acknowledgements](spec/annex-software-work-integrity-1.0-rc3.md#L167) | The independent OWP validator MUST acknowledge **preflight verifiability**: the machine/evidence criteria are sufficiently precise to validate, the required source and execution evidence can be obtained independently, and no criterion requires trusting a provider assertion as its own proof. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L176-MUST-NOT-1 | MUST NOT | [B.4.2 Contract digest and acknowledgements](spec/annex-software-work-integrity-1.0-rc3.md#L176) | A contract MUST NOT be considered accepted when any required role is absent, uses a different `contract_digest`, or returns anything other than `ACCEPT`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L185-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L185) | A Work Agreement defines the outcome across Attempts. The exact starting Git state can change on every revision or provider handoff. Therefore the immutable source snapshot MUST be bound to **each Attempt**, not only to the Work Agreement. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L189-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L189) | Before an Attempt begins, the customer/provider/orchestrator/validator workflow MUST produce an `execution_commitment` containing at least: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L211-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L211) | `object_format` MUST be `sha1` or `sha256` for this version of the Git profile. A SHA-1 object identifier MUST contain exactly 40 lowercase hexadecimal characters. A SHA-256 object identifier MUST contain exactly 64 lowercase hexadecimal characters. Abbreviated object identifiers MUST NOT be used in a normative execution commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L212-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L212) | `object_format` MUST be `sha1` or `sha256` for this version of the Git profile. A SHA-1 object identifier MUST contain exactly 40 lowercase hexadecimal characters. A SHA-256 object identifier MUST contain exactly 64 lowercase hexadecimal characters. Abbreviated object identifiers MUST NOT be used in a normative execution commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L213-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L213) | `object_format` MUST be `sha1` or `sha256` for this version of the Git profile. A SHA-1 object identifier MUST contain exactly 40 lowercase hexadecimal characters. A SHA-256 object identifier MUST contain exactly 64 lowercase hexadecimal characters. Abbreviated object identifiers MUST NOT be used in a normative execution commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L214-MUST-NOT-1 | MUST NOT | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L214) | `object_format` MUST be `sha1` or `sha256` for this version of the Git profile. A SHA-1 object identifier MUST contain exactly 40 lowercase hexadecimal characters. A SHA-256 object identifier MUST contain exactly 64 lowercase hexadecimal characters. Abbreviated object identifiers MUST NOT be used in a normative execution commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L217-MUST-1 | MUST | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L217) | The source snapshot MUST identify the exact commit **and** tree expected before execution. A branch, tag, pull-request number, symbolic ref, or moving name MAY be retained as human context but MUST NOT replace the immutable object IDs. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L218-MAY-1 | MAY | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L218) | The source snapshot MUST identify the exact commit **and** tree expected before execution. A branch, tag, pull-request number, symbolic ref, or moving name MAY be retained as human context but MUST NOT replace the immutable object IDs. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L219-MUST-NOT-1 | MUST NOT | [B.5 Attempt-specific execution commitment](spec/annex-software-work-integrity-1.0-rc3.md#L219) | The source snapshot MUST identify the exact commit **and** tree expected before execution. A branch, tag, pull-request number, symbolic ref, or moving name MAY be retained as human context but MUST NOT replace the immutable object IDs. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L228-MUST-1 | MUST | [B.5.1 Attempt acceptance](spec/annex-software-work-integrity-1.0-rc3.md#L228) | The Work Agreement establishes the shared Definition of Done, but it does not by itself accept a moving source snapshot or a later paid STEER. Before **every** Attempt, all four agreement roles MUST acknowledge the same `execution_commitment_digest`: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L250-MUST-NOT-1 | MUST NOT | [B.5.1 Attempt acceptance](spec/annex-software-work-integrity-1.0-rc3.md#L250) | An Attempt MUST NOT be treated as accepted, funded for execution, or started under this profile when any role is absent, acknowledges a different digest, or returns anything other than `ACCEPT`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L255-SHOULD-1 | SHOULD | [B.5.1 Attempt acceptance](spec/annex-software-work-integrity-1.0-rc3.md#L255) | Payment receipts, spending mandates, capability grants, or other authority artifacts SHOULD bind to `execution_commitment_digest`; they MUST NOT be placed inside the execution commitment merely to make the commitment depend on a receipt that itself depends on the commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L255-MUST-NOT-2 | MUST NOT | [B.5.1 Attempt acceptance](spec/annex-software-work-integrity-1.0-rc3.md#L255) | Payment receipts, spending mandates, capability grants, or other authority artifacts SHOULD bind to `execution_commitment_digest`; they MUST NOT be placed inside the execution commitment merely to make the commitment depend on a receipt that itself depends on the commitment. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L259-MUST-NOT-1 | MUST NOT | [B.5.1 Attempt acceptance](spec/annex-software-work-integrity-1.0-rc3.md#L259) | A provider or orchestrator MUST NOT begin the Attempt until the source snapshot is fixed, all four Attempt acknowledgements are present, and the execution commitment digest is available to all parties that must authorize, execute, or validate the Attempt. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L266-MUST-1 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L266) | For revision zero, `parent` MUST be `null`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L268-MUST-1 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L268) | For a later revision or cross-provider handoff, `parent` MUST contain: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L278-MUST-1 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L278) | The new `source_snapshot.base_commit` and `source_snapshot.base_tree` MUST equal the exact validated result commit and tree named by the parent validator attestation. This equality is the handoff trust boundary. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L283-MUST-NOT-1 | MUST NOT | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L283) | A provider-reported prior `result_commit`, branch tip, PR head, workspace, or handoff prose MUST NOT substitute for the validator-attested result. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L285-MAY-1 | MAY | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L285) | A paid STEER MAY add an `acceptance_delta`. Each delta item MUST use the same Definition-of-Done item shape as B.4.1 and MUST be inside the hashed execution commitment. A validator MUST execute or verify the effective acceptance set: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L285-MUST-2 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L285) | A paid STEER MAY add an `acceptance_delta`. Each delta item MUST use the same Definition-of-Done item shape as B.4.1 and MUST be inside the hashed execution commitment. A validator MUST execute or verify the effective acceptance set: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L286-MUST-1 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L286) | A paid STEER MAY add an `acceptance_delta`. Each delta item MUST use the same Definition-of-Done item shape as B.4.1 and MUST be inside the hashed execution commitment. A validator MUST execute or verify the effective acceptance set: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L287-MUST-1 | MUST | [B.5.2 Revisions and handoffs](spec/annex-software-work-integrity-1.0-rc3.md#L287) | A paid STEER MAY add an `acceptance_delta`. Each delta item MUST use the same Definition-of-Done item shape as B.4.1 and MUST be inside the hashed execution commitment. A validator MUST execute or verify the effective acceptance set: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L316-MUST-NOT-1 | MUST NOT | [B.6 Provider delivery claim](spec/annex-software-work-integrity-1.0-rc3.md#L316) | A delivery claim is untrusted input. Its Git object IDs, check results, artifact hashes, CI links, and evidence references MUST NOT become trusted merely because they are well-formed, signed by the provider, recorded in an OWP event, or stored on a protected branch. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L320-MUST-1 | MUST | [B.6 Provider delivery claim](spec/annex-software-work-integrity-1.0-rc3.md#L320) | The provider's `claimed_base_commit` MUST equal the execution commitment's `source_snapshot.base_commit`; a mismatch is `INVALID` without attempting to reinterpret the provider's intent. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L328-MUST-1 | MUST | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L328) | The independent validator MUST validate from its own controlled environment. It MUST NOT treat the provider's working directory, provider-controlled cache, or provider-supplied checkout as the authoritative validation workspace. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L329-MUST-NOT-1 | MUST NOT | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L329) | The independent validator MUST validate from its own controlled environment. It MUST NOT treat the provider's working directory, provider-controlled cache, or provider-supplied checkout as the authoritative validation workspace. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L333-MUST-1 | MUST | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L333) | For a Git-backed Attempt that can be validated deterministically, the validator MUST perform the following checks or an equivalent set that proves the same properties: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L349-MUST-1 | MUST | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L349) | 9. Verify the result is derived from the expected base according to the contract's history policy. At minimum, where ordinary linear ancestry is the declared policy, the base MUST be an ancestor of the result. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L358-MUST-NOT-1 | MUST NOT | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L358) | 14. Verify every evidence-backed item and label every human-only item as human; the validator MUST NOT synthesize a machine pass for a human criterion. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L365-MUST-1 | MUST | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L365) | If the validator cannot independently obtain the required source, cannot run a required deterministic check, cannot establish the exact test subject, or lacks required isolation, it MUST return `INDETERMINATE`, not `VALID`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L369-MUST-1 | MUST | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L369) | A failed deterministic requirement, source mismatch, ancestry violation, tree substitution, forbidden-path change, contract mismatch, Attempt mismatch, or result/test subject mismatch MUST return `INVALID`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L371-MAY-1 | MAY | [B.7 Independent Git validation](spec/annex-software-work-integrity-1.0-rc3.md#L371) | A profile implementation MAY impose a stricter history policy, including single-parent linear history, merge restrictions, commit-signature policy, source-control provenance, two-party review, or SLSA Source levels. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L395-MUST-1 | MUST | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L395) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L396-MUST-1 | MUST | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L396) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L397-MUST-1 | MUST | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L397) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L398-MUST-NOT-1 | MUST NOT | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L398) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L399-MUST-1 | MUST | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L399) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L400-MUST-1 | MUST | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L400) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L400-MUST-NOT-2 | MUST NOT | [B.7.1 Portable change-set digest](spec/annex-software-work-integrity-1.0-rc3.md#L400) | Absent sides of an add/delete are `null`. Profile-v1 changed paths MUST be valid UTF-8 strings. Validators MUST reject a path that cannot round-trip as UTF-8 and MUST sort change-set entries by the unsigned lexicographic byte order of the UTF-8 encoding of `path`; locale collation MUST NOT be used. `diff_digest` is the B.3 canonical SHA-256 digest of this array. Validators MUST derive the entries from the exact base/result trees, MUST use full object ids, and MUST NOT let local rename, textconv, external-diff, replacement-object, or graft configuration redefine the identity being checked. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L411-MUST-1 | MUST | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L411) | Every reusable deterministic test/build result MUST identify the exact artifact it evaluated. For Git source, the result SHOULD use an in-toto Statement whose subject digest includes `gitCommit` and SHOULD also include `gitTree`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L412-SHOULD-1 | SHOULD | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L412) | Every reusable deterministic test/build result MUST identify the exact artifact it evaluated. For Git source, the result SHOULD use an in-toto Statement whose subject digest includes `gitCommit` and SHOULD also include `gitTree`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L413-SHOULD-1 | SHOULD | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L413) | Every reusable deterministic test/build result MUST identify the exact artifact it evaluated. For Git source, the result SHOULD use an in-toto Statement whose subject digest includes `gitCommit` and SHOULD also include `gitTree`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L416-SHOULD-1 | SHOULD | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L416) | The in-toto Test Result predicate `https://in-toto.io/attestation/test-result/v0.1` SHOULD be used for ordinary test results when it can express the evidence. The statement subject MUST equal the validator-recomputed result commit/tree. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L417-MUST-1 | MUST | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L417) | The in-toto Test Result predicate `https://in-toto.io/attestation/test-result/v0.1` SHOULD be used for ordinary test results when it can express the evidence. The statement subject MUST equal the validator-recomputed result commit/tree. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L420-MUST-NOT-1 | MUST NOT | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L420) | A test performed on commit B MUST NOT be accepted as evidence for delivered commit C merely because B and C are close, share a branch, or have the same provider. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L424-MAY-1 | MAY | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L424) | Build provenance MAY additionally use SLSA Build Provenance. Source-control trust MAY use SLSA Source provenance or Source Verification Summary Attestations. OWP does not redefine SLSA levels. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L425-MAY-1 | MAY | [B.8 Test/build subject binding](spec/annex-software-work-integrity-1.0-rc3.md#L425) | Build provenance MAY additionally use SLSA Build Provenance. Source-control trust MAY use SLSA Source provenance or Source Verification Summary Attestations. OWP does not redefine SLSA levels. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L432-SHOULD-1 | SHOULD | [B.9 Validator attestation](spec/annex-software-work-integrity-1.0-rc3.md#L432) | The validator SHOULD emit an in-toto Statement v1 with predicate type: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L436-MUST-1 | MUST | [B.9 Validator attestation](spec/annex-software-work-integrity-1.0-rc3.md#L436) | The Statement subject MUST identify the exact validated result commit and tree. The predicate MUST include at least: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L437-MUST-1 | MUST | [B.9 Validator attestation](spec/annex-software-work-integrity-1.0-rc3.md#L437) | The Statement subject MUST identify the exact validated result commit and tree. The predicate MUST include at least: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L455-MUST-1 | MUST | [B.9 Validator attestation](spec/annex-software-work-integrity-1.0-rc3.md#L455) | `validator_attestation_digest` is the canonical digest of the complete attestation object or, when a signed attestation envelope is used, the digest of the immutable attestation object identified by that envelope. An implementation MUST document which object the digest names and MUST verify it consistently. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L455-MUST-2 | MUST | [B.9 Validator attestation](spec/annex-software-work-integrity-1.0-rc3.md#L455) | `validator_attestation_digest` is the canonical digest of the complete attestation object or, when a signed attestation envelope is used, the digest of the immutable attestation object identified by that envelope. An implementation MUST document which object the digest names and MUST verify it consistently. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L477-MUST-1 | MUST | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L477) | `decision` MUST be `APPROVE`, `STEER`, or `REJECT`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L481-MAY-1 | MAY | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L481) | An `APPROVE` decision MAY allow the operator to accept the pending core CompletionRecord and let the deliverable exit `review` as `completed`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L484-MUST-1 | MUST | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L484) | A `STEER` decision creates a new Attempt. Its source snapshot MUST begin at the prior validator-attested result, and its `parent` MUST bind both the prior validator attestation and the STEER disposition. New objective acceptance requirements MUST appear in the new Attempt's `acceptance_delta`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L485-MUST-1 | MUST | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L485) | A `STEER` decision creates a new Attempt. Its source snapshot MUST begin at the prior validator-attested result, and its `parent` MUST bind both the prior validator attestation and the STEER disposition. New objective acceptance requirements MUST appear in the new Attempt's `acceptance_delta`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L487-MUST-1 | MUST | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L487) | A `STEER` decision creates a new Attempt. Its source snapshot MUST begin at the prior validator-attested result, and its `parent` MUST bind both the prior validator attestation and the STEER disposition. New objective acceptance requirements MUST appear in the new Attempt's `acceptance_delta`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L493-MUST-1 | MUST | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L493) | A handoff package MUST name the validator-attested current commit/tree and the attestation digest. It MUST NOT promote a provider-reported branch tip or result SHA to portable trusted state without independent validation. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L494-MUST-NOT-1 | MUST NOT | [B.10 Customer disposition and handoff](spec/annex-software-work-integrity-1.0-rc3.md#L494) | A handoff package MUST name the validator-attested current commit/tree and the attestation digest. It MUST NOT promote a provider-reported branch tip or result SHA to portable trusted state without independent validation. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L503-MUST-NOT-1 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L503) | A successful payment MUST NOT be represented as proof that software passed validation. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L505-MUST-NOT-1 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L505) | A validator `VALID` result MUST NOT be represented as proof that payment was authorized or settled. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L507-MUST-NOT-1 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L507) | A reputation score MUST NOT replace exact delivery evidence for the current Attempt. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L510-SHOULD-1 | SHOULD | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L510) | Where x402 v2 is used, OWP metadata SHOULD be carried in the protocol's extension fields and SHOULD bind the payment requirement/payload/settlement record to `execution_commitment_digest`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L511-SHOULD-1 | SHOULD | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L511) | Where x402 v2 is used, OWP metadata SHOULD be carried in the protocol's extension fields and SHOULD bind the payment requirement/payload/settlement record to `execution_commitment_digest`. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L514-SHOULD-1 | SHOULD | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L514) | Where AP2 is used, any commerce-layer mandate/receipt binding SHOULD reference the same execution commitment when the containing commerce protocol provides a safe extension point. OWP does not redefine AP2 mandates, payment roles, or deterministic validation responsibilities. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L519-MAY-1 | MAY | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L519) | Where ERC-8004 is used, identity, reputation, or validation registry entries MAY reference OWP validator evidence. OWP MUST NOT define one universal on-chain quality score. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L520-MUST-NOT-1 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L520) | Where ERC-8004 is used, identity, reputation, or validation registry entries MAY reference OWP validator evidence. OWP MUST NOT define one universal on-chain quality score. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L523-MUST-1 | MUST | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L523) | Where A2A 1.0 transports provider work, the OWP profile MUST use an A2A extension URI rather than redefining A2A core fields. A2A `taskId` values are server-generated and MUST NOT be replaced by an OWP work ref or Attempt id. OWP identifiers and digests SHOULD ride in typed extension metadata. A2A `contextId` MAY group related work interactions but MUST NOT be treated as the portable OWP work identity. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L525-MUST-NOT-1 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L525) | Where A2A 1.0 transports provider work, the OWP profile MUST use an A2A extension URI rather than redefining A2A core fields. A2A `taskId` values are server-generated and MUST NOT be replaced by an OWP work ref or Attempt id. OWP identifiers and digests SHOULD ride in typed extension metadata. A2A `contextId` MAY group related work interactions but MUST NOT be treated as the portable OWP work identity. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L526-SHOULD-1 | SHOULD | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L526) | Where A2A 1.0 transports provider work, the OWP profile MUST use an A2A extension URI rather than redefining A2A core fields. A2A `taskId` values are server-generated and MUST NOT be replaced by an OWP work ref or Attempt id. OWP identifiers and digests SHOULD ride in typed extension metadata. A2A `contextId` MAY group related work interactions but MUST NOT be treated as the portable OWP work identity. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L527-MAY-1 | MAY | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L527) | Where A2A 1.0 transports provider work, the OWP profile MUST use an A2A extension URI rather than redefining A2A core fields. A2A `taskId` values are server-generated and MUST NOT be replaced by an OWP work ref or Attempt id. OWP identifiers and digests SHOULD ride in typed extension metadata. A2A `contextId` MAY group related work interactions but MUST NOT be treated as the portable OWP work identity. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L527-MUST-NOT-2 | MUST NOT | [B.11 Commerce, authority, reputation, and transport composition](spec/annex-software-work-integrity-1.0-rc3.md#L527) | Where A2A 1.0 transports provider work, the OWP profile MUST use an A2A extension URI rather than redefining A2A core fields. A2A `taskId` values are server-generated and MUST NOT be replaced by an OWP work ref or Attempt id. OWP identifiers and digests SHOULD ride in typed extension metadata. A2A `contextId` MAY group related work interactions but MUST NOT be treated as the portable OWP work identity. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L557-MUST-NOT-1 | MUST NOT | [B.12 Core OWP lifecycle integration](spec/annex-software-work-integrity-1.0-rc3.md#L557) | A deployment claiming this profile MUST NOT configure a straight-to-exit completion path for work whose contract requires independent validation unless an independently produced `VALID` attestation for that exact result already exists. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L566-MUST-1 | MUST | [B.13 Threats this profile is required to detect](spec/annex-software-work-integrity-1.0-rc3.md#L566) | A conforming validator test suite MUST include negative coverage for at least: | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L594-MUST-1 | MUST | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L594) | The validator MUST treat repositories and build inputs as hostile. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L596-SHOULD-1 | SHOULD | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L596) | Validation SHOULD run in a disposable sandbox or an equivalently isolated environment with no ambient user credentials. Secrets not explicitly required by the validation policy MUST NOT be exposed to the result under test. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L598-MUST-NOT-1 | MUST NOT | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L598) | Validation SHOULD run in a disposable sandbox or an equivalently isolated environment with no ambient user credentials. Secrets not explicitly required by the validation policy MUST NOT be exposed to the result under test. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L600-SHOULD-1 | SHOULD | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L600) | A validator SHOULD fetch source and dependencies with least privilege and SHOULD retain only the evidence required by the Work Agreement and applicable policy. Sensitive evidence MAY be content-addressed and access-controlled rather than embedded directly in OWP records. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L600-SHOULD-2 | SHOULD | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L600) | A validator SHOULD fetch source and dependencies with least privilege and SHOULD retain only the evidence required by the Work Agreement and applicable policy. Sensitive evidence MAY be content-addressed and access-controlled rather than embedded directly in OWP records. | Deployments selecting the Software Work Integrity Profile | tools/test-software-work-integrity.mjs; tools/test-software-work-integrity-vectors.mjs; schemas/software-work-*.schema.json; profile prose review. | PASS | Spec-local profile invariants are executable; external independent provider/build-sandbox interoperability remains a release gate. |
| SWI-L602-MAY-1 | MAY | [B.14 Privacy and secret handling](spec/annex-software-work-integrity-1.0-rc3.md#L602) | A validator SHOULD fetch source and dependencies with least privilege and SHOULD retain only the evidence required by the Work Agreement and applicable policy. Sensitive evidence MAY be content-addressed and access-controlled rather than embedded directly in OWP records. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| SWI-L612-MAY-1 | MAY | [B.15 Conformance claim](spec/annex-software-work-integrity-1.0-rc3.md#L612) | A deployment MAY claim `OWP Software Work Integrity Profile v1` only when: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; no implementation is required solely by this MAY. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
