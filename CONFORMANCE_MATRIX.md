# Normative requirement and conformance matrix

Revision: **1.0-rc2**  
Generated: **2026-08-08** by `tools/build-requirement-matrix.mjs`

This inventory contains every RFC 2119 keyword occurrence in the normative
parts of the specification and HTTP annex. Sections 21–22 are historical and
informative and are deliberately excluded. One row is emitted per keyword,
including multiple obligations on one source line. IDs pin the immutable RC2
file and line; regenerate and review the diff if normative text changes.

Counts: **MUST 76** · **MUST NOT 30** · **SHOULD 18** · **SHOULD NOT 0** · **MAY 21** · **total 145**.

`PASS` means the named evidence was reviewed in this release run. `N/A
(permission)` is the justified designation for a MAY. Conditional requirements
name the condition and do not count as exercised when that condition is absent.
The tests prove one reference implementation; they do not prove independent
interoperability.

| ID | Modality | Specification location | Requirement text | Applicable components | Automated or review evidence | Result | Remaining gap |
|---|---|---|---|---|---|---|---|
| OWP-L37-MUST-1 | MUST | [1. Purpose](spec/owp-1.0-rc2.md#L37) | A conforming deployment MUST preserve four properties. They are the protocol's purpose stated as tests: | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L42-MUST-NOT-1 | MUST NOT | [1. Purpose](spec/owp-1.0-rc2.md#L42) | **§1.1 — No agent blocks on a human.** Asking a question never stops work (§12). An agent that genuinely cannot proceed releases the work and takes other work; it MUST NOT idle holding a claim (§7.5). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L49-MUST-1 | MUST | [1. Purpose](spec/owp-1.0-rc2.md#L49) | **§1.3 — A human decision is cheap to give and expensive to be absent for.** Every agent question MUST carry options and evidence sufficient to decide from the card alone (§12.1). The obligation is deliberately asymmetric: assembling evidence is cheap for machines and expensive for humans. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L100-MUST-1 | MUST | [3. The line: envelopes and vocabularies](spec/owp-1.0-rc2.md#L100) | An **envelope** is a structure the surface stores, round-trips, and never interprets. Where the protocol needs open-endedness it MUST use an envelope rather than an enumeration: | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L118-MUST-NOT-1 | MUST NOT | [3. The line: envelopes and vocabularies](spec/owp-1.0-rc2.md#L118) | **An example may extend inside an envelope freely; it MUST NOT grow spec-shaped machinery of its own.** What cannot be expressed inside an envelope is protocol feedback, not a local hack. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L128-MUST-NOT-1 | MUST NOT | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L128) | **4.1 Frozen at 1.0.** The following MUST NOT change without a major revision: the four §1 properties; the surface-computes/clients-mutate-and-author invariant; the §3 envelope discipline; the verb names in §10 and their semantics; the state machine (§8); the fixed rank tiers (§11); the authority classes (§18); and the meaning of `record-only`. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| OWP-L136-MUST-1 | MUST | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L136) | **4.2 Additive without a revision bump.** New registered envelope values, new optional verbs, new optional fields, new observations, and new features declared through `surface.describe` (§9). A client MUST tolerate unknown fields and unknown envelope values (§19). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L139-SHOULD-1 | SHOULD | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L139) | A revision SHOULD publish a **machine-readable schema** alongside this prose (the reference implementation emits one from its single definition: `owp-code/schema/`). Where the two disagree, **the prose wins** — the schema is a convenience for generating types and validating payloads, not a second source of truth. Envelope *values* are deliberately absent from it: they belong to the deployment and are discovered through §9, not fixed in a file. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| OWP-L150-MAY-1 | MAY | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L150) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L151-MUST-1 | MUST | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L151) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L152-MUST-NOT-1 | MUST NOT | [4. Compatibility and stability](spec/owp-1.0-rc2.md#L152) | **4.4 `[experimental]`.** A section marked experimental MAY change or be removed in any revision. A surface MUST declare whether it implements one via `surface.describe().features`, and a client MUST NOT assume it. Exactly one section is experimental in 1.0: §12.6, upstream questions. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L167-MUST-NOT-1 | MUST NOT | [5. Roles](spec/owp-1.0-rc2.md#L167) | **Surface** — the server. Holds the working set, serves projections, assigns event order, computes derivations. It MUST NOT act on its own initiative. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L175-SHOULD-1 | SHOULD | [5. Roles](spec/owp-1.0-rc2.md#L175) | **Creator clients** — schedulers, watchers, janitors: ordinary clients that create proposals, unpark on triggers, release claims, or report observations, each per its own policy. **A creator client SHOULD own its own memory as well as its own clock**: idempotency ("did I already propose 16.0?") comes from a cursor the client keeps, never from querying the surface — a correct creator client needs zero read authority `[ops]`. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L184-MUST-NOT-1 | MUST NOT | [5. Roles](spec/owp-1.0-rc2.md#L184) | **Role routing is experience-side.** Deployments with several humans (an author and an editor; a reviewer and an approver) express "whose turn" in their operator client and in `park.trigger` prose. The protocol MUST NOT gain a human assignee field: it would import the human workflow OWP exists to avoid `[publish]`. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L216-MUST-1 | MUST | [7.1 Project](spec/owp-1.0-rc2.md#L216) | `key` prefixes every ref (`PAY-4`). **Refs are the only identifier anyone — human, agent, or UI — speaks, and MUST render identically everywhere.** `vocabulary` is the deployment's published envelope values (§9, §20). | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L244-MUST-1 | MUST | [7.2 Deliverable](spec/owp-1.0-rc2.md#L244) | **`state` above is the working set.** An item that leaves it (§8) keeps reporting a state, and reads MUST be able to say which: `completed` is §8's own exit, a proposal closed `record-only` (§12.3) reports `rejected`, and an explicitly withdrawn live item reports `cancelled` (§8). All three are terminal — no transition leaves them. They are defined by the protocol so every conforming client can parse all valid responses consistently. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L249-MUST-NOT-1 | MUST NOT | [7.2 Deliverable](spec/owp-1.0-rc2.md#L249) | `parent` is delegation only; depth MUST NOT exceed 2. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L267-MUST-1 | MUST | [7.3 Links](spec/owp-1.0-rc2.md#L267) | The envelope is normative; the vocabulary is not. Surfaces **MUST preserve (round-trip) link types they do not understand.** `references` is the one registered type, because promotion and provenance depend on it (§12.4). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L277-SHOULD-1 | SHOULD | [7.3 Links](spec/owp-1.0-rc2.md#L277) | **Write semantics (normative).** `work.update(ref, {links})` **replaces each named type's entry array wholesale**; types not named are preserved untouched. A client patching a multi-entry type therefore supplies the complete set — which a creator client holds in its own memory (§5), never by reading the surface. Per-entry merge would require the surface to know an entry's identity key, which is vocabulary and therefore out; deployments whose entries have independent writers SHOULD model finer-grained deliverables instead `[ops]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L287-MUST-NOT-1 | MUST NOT | [7.4 Event](spec/owp-1.0-rc2.md#L287) | `seq` is a surface-assigned **total order**. Clients MUST NOT order by timestamp. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L289-MAY-1 | MAY | [7.4 Event](spec/owp-1.0-rc2.md#L289) | Reads are cursor-based (`since: seq`) and MAY be bounded by `limit`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L290-MUST-1 | MUST | [7.4 Event](spec/owp-1.0-rc2.md#L290) | Reads are cursor-based (`since: seq`) and MAY be bounded by `limit`. - **`actor`** is a registered session id **or** a client identity. Clients MUST be able to distinguish the two; a session id resolves through `session.view` (§15) and a client identity does not. *(`session` is emitted as a deprecated alias for one revision — §4.5.)* | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L294-MUST-NOT-1 | MUST NOT | [7.4 Event](spec/owp-1.0-rc2.md#L294) | **The surface MUST NOT author events whose body is prose.** State transitions are recorded as state. A surface appends exactly three kinds on its own: `unparked` (§7.5, body is data), `completed` (§13, body is the record), and `cancelled` (§8, body is the client's required reason). Narration is a client's business. *An editorial desk, where notes are the product, cannot distinguish machine narration from an editor's note* `[publish]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L315-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc2.md#L315) | 1. **Parking releases ownership.** `owner_session` clears. 2. **Parking MUST include the handoff payload** — all four fields. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L318-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc2.md#L318) | 4. `question` names the gating question for `cause: decision`; if present, only a decision on *that* question releases the park. **The question MUST be open on this item** — a surface rejects a park naming one that is not (`STATE`), and a decision releases parks on its own item only (§12.2). | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L326-MUST-1 | MUST | [7.5 Parked](spec/owp-1.0-rc2.md#L326) | 6. The release MUST emit an `unparked` event carrying the park payload, so the handoff survives into the resuming session's assignment packet. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L353-MUST-1 | MUST | [7.7 CompletionRecord](spec/owp-1.0-rc2.md#L353) | `outcome` MUST be present; the rest default to empty. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L372-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L372) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L373-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L373) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L373-MUST-2 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L373) | `work.cancel(ref, reason)` MUST require operator authority or a grant naming the verb, MUST require a non-empty reason, and MUST append a `cancelled` event attributed to the caller before the item leaves the working set. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L375-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L375) | A surface MUST refuse a non-completed exit (`cancelled` or `rejected`) with `STATE` while any non-exited item directly depends on the target. The client cancels or rewires downstream work first; silently stranding it would violate the chooser's dependency contract. For the same reason, `work.create` and `work.update` MUST refuse `depends_on` entries already `cancelled` or `rejected`; only a live target or `completed` exit can ever satisfy the edge. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L379-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L379) | A surface MUST refuse a non-completed exit (`cancelled` or `rejected`) with `STATE` while any non-exited item directly depends on the target. The client cancels or rewires downstream work first; silently stranding it would violate the chooser's dependency contract. For the same reason, `work.create` and `work.update` MUST refuse `depends_on` entries already `cancelled` or `rejected`; only a live target or `completed` exit can ever satisfy the edge. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L381-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L381) | Cancellation MUST clear ownership, park and pending completion data. A holder's `current_item` MUST also clear. A second cancellation is `STATE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L382-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L382) | Cancellation MUST clear ownership, park and pending completion data. A holder's `current_item` MUST also clear. A second cancellation is `STATE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L383-MUST-1 | MUST | [8. State and observations](spec/owp-1.0-rc2.md#L383) | Cancellation is not deletion. `work.view` MUST continue to return the record, reasoned event, and terminal state. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L397-MUST-NOT-1 | MUST NOT | [8. State and observations](spec/owp-1.0-rc2.md#L397) | `unreconciled` is envelope-shaped: a **watcher client** reports what it observed into any link entry; the surface derives the observation from data it already holds; a session clears it by writing the worklog forward. **The surface MUST NOT read the outside world** `[ops]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L402-MUST-NOT-1 | MUST NOT | [8. State and observations](spec/owp-1.0-rc2.md#L402) | **The surface never acts on an observation.** There is no expiry, no auto-release, and no protocol-level threshold — what reads as "stalled" is deployment configuration. Recovery of work held by a dead session is a **client** action. Elapsed time exists only as reported observations and MUST NOT schedule or mutate anything. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L411-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L411) | **`surface.describe()`** is the one bootstrap read. It MUST be callable without a session. A credentialed binding MAY require a valid credential; if it does, missing or invalid credentials answer `UNAUTHENTICATED` like any other verb. It returns an envelope whose slots the spec names and whose values the deployment fills: | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L412-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc2.md#L412) | **`surface.describe()`** is the one bootstrap read. It MUST be callable without a session. A credentialed binding MAY require a valid credential; if it does, missing or invalid credentials answer `UNAUTHENTICATED` like any other verb. It returns an envelope whose slots the spec names and whose values the deployment fills: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L432-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L432) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L432-MUST-2 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L432) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L434-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L434) | A surface MUST report the revisions it supports, and MUST answer `UNSUPPORTED_VERSION` (§16) to a client that pins one it does not implement. **How a client pins is a binding concern** — a binding MUST provide some mechanism and state it; the HTTP annex uses a request header (§A.5). Absent a pin, a surface answers in its default revision, which this call reports. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L437-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L437) | A surface MUST report an authenticated or open-mode caller's **own** authority class and grants. A client learning its authority by receiving a `FORBIDDEN` is the failure this removes. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L441-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L441) | A surface MUST list the verbs it implements, so optional features are discoverable rather than probed. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L443-MUST-1 | MUST | [9. Discovery](spec/owp-1.0-rc2.md#L443) | A conforming deployment MUST publish its vocabulary here (§20). This is §3's obligation made machine-readable. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L445-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc2.md#L445) | A surface with many projects MAY report only those the caller may act on, and MAY omit `projects` entirely for an unauthenticated caller. Discovery is a bootstrap read, not a directory: a client that needs the full portfolio calls `portfolio` (§15), which is scoped and bounded. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L446-MAY-1 | MAY | [9. Discovery](spec/owp-1.0-rc2.md#L446) | A surface with many projects MAY report only those the caller may act on, and MAY omit `projects` entirely for an unauthenticated caller. Discovery is a bootstrap read, not a directory: a client that needs the full portfolio calls `portfolio` (§15), which is scoped and bounded. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L461-SHOULD-1 | SHOULD | [Agent-side (require a registered session)](spec/owp-1.0-rc2.md#L461) | \| `session.heartbeat()` \| Cheap liveness; SHOULD piggyback on every verb \| | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L462-MUST-NOT-1 | MUST NOT | [Agent-side (require a registered session)](spec/owp-1.0-rc2.md#L462) | \| `session.end()` \| Marks the session ended. MUST NOT release its claims — that is a client decision (§8) \| | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L466-MUST-NOT-1 | MUST NOT | [Agent-side (require a registered session)](spec/owp-1.0-rc2.md#L466) | \| `work.create(item, dispatch)` → ref \| `dispatch:true` ⇒ todo; `false` ⇒ `proposed`. Dispatch MUST NOT cross projects \| | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L472-MUST-1 | MUST | [Agent-side (require a registered session)](spec/owp-1.0-rc2.md#L472) | \| `policy.applicable(ref)` → policies \| All active policies in scope; MUST be consulted before irreversible actions \| | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L504-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L504) | `work.next` is the most-called verb and MUST be deterministic enough that two conforming surfaces hand the same session the same work. **`work.next` chooses AND claims atomically**; a choose-then-claim split races two sessions onto one item. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L520-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L520) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L520-MUST-2 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L520) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L521-MUST-NOT-1 | MUST NOT | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L521) | `pin` is the operator's ordinal and dominates. `urgency` is blocking > elevated > routine, with recently-released parks counting as elevated. The **declared tiebreak** is the deployment's one slot (§3); surfaces MUST apply a declared tiebreak they understand, MUST ignore one they do not, and MUST NOT reorder the fixed tiers. **`creation ordinal`** is the surface's own monotonic order — not a timestamp — and makes the ranking **total**: batches that become eligible together (a reground landing forty items, a triage sweep, a park release) would otherwise have no defined order, which breaks paging and interoperability alike. At volume that batch is the normal case, not the corner: 150 items created in a few milliseconds tie on every other key, so this tier decides everything `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L539-MUST-1 | MUST | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L539) | **The ordinal MUST be observable**, or §20 cannot check the tier that carries §11's interoperability promise. It already is: refs are `KEY-N` with `N` assigned in creation order within a project (§7.1), and the chooser only ever ranks within one project — so `N` *is* the ordinal, and any client can verify the ordering from the refs it was handed. A surface MUST NOT use a different ordinal than the one its refs expose. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L543-MUST-NOT-1 | MUST NOT | [11. Assignment — the work.next chooser](spec/owp-1.0-rc2.md#L543) | **The ordinal MUST be observable**, or §20 cannot check the tier that carries §11's interoperability promise. It already is: refs are `KEY-N` with `N` assigned in creation order within a project (§7.1), and the chooser only ever ranks within one project — so `N` *is* the ordinal, and any client can verify the ordering from the refs it was handed. A surface MUST NOT use a different ordinal than the one its refs expose. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L550-MUST-1 | MUST | [12.1 Questions](spec/owp-1.0-rc2.md#L550) | An agent `question` body MUST carry `options[]`, and **every option MUST carry `evidence[]`** — entries are envelopes (§3) and round-trip: | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L550-MUST-2 | MUST | [12.1 Questions](spec/owp-1.0-rc2.md#L550) | An agent `question` body MUST carry `options[]`, and **every option MUST carry `evidence[]`** — entries are envelopes (§3) and round-trip: | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L568-MAY-1 | MAY | [12.1 Questions](spec/owp-1.0-rc2.md#L568) | **A question body MAY carry `direction`** (`to_operator` \| `to_session`). When it is absent a surface infers the old default — a registered session's question runs downstream to the operator, anyone else's runs upstream (§12.6) — so nothing already written changes meaning. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L580-MUST-1 | MUST | [12.1 Questions](spec/owp-1.0-rc2.md#L580) | Stating it matters because one bit, *"is the author a registered session?"*, was deciding two independent things: the evidence obligation and the routing direction. They are not the same question, and a creator client is the proof `[publish]`. A watcher is a machine — cheap for it to assemble options and evidence, so §12.1's obligation should bind it — that needs to ask a **human**, and it had no way to say so: its class routed its question to a session, and it had no session. **The obligation stays attached to the author's class; only the routing moved.** A surface MUST honour a stated `direction` in both places the routing shows: the attention queue (§15) and the assignment packet (§10). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L589-MUST-1 | MUST | [12.2 Decisions](spec/owp-1.0-rc2.md#L589) | **A decision is item-local.** The question gating a decision-park MUST be open on the parked item itself (§7.5 rule 4), and answering it releases parks on **that item only** — never every park elsewhere that happens to name the same seq. Both ends were unstated, and two conforming surfaces could read the same sentences opposite ways, with no test to tell them apart `[publish]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L605-MUST-1 | MUST | [12.3 Continuations](spec/owp-1.0-rc2.md#L605) | **Every rejection MUST name its continuation.** The vocabulary is an envelope with exactly one registered value: | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L615-MAY-1 | MAY | [12.4 Promotion](spec/owp-1.0-rc2.md#L615) | **One mechanism, two targets.** Any decision artifact — an option, an answer, a rejection, an editorial note — MAY be promoted into a **proposal** or a **policy**, with `provenance` naming the originating event and item. The provenance chain is the point: it is how runbooks, style guides, and tried-and-rejected lists write themselves `[publish][ops]`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L622-MAY-1 | MAY | [12.5 The operator's voice](spec/owp-1.0-rc2.md#L622) | An operator or creator client MAY append `note` events to any item. A note is **context without a verdict**: nothing changes state, and the note rides the worklog into whichever session works the item next. Client actors are restricted to `note` and `question`; answers go through `answer`, verdicts through `reject`/`triage`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L630-MAY-1 | MAY | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc2.md#L630) | An operator **or a creator client** MAY author a `question` on an item. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L637-SHOULD-1 | SHOULD | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc2.md#L637) | The evidence obligation of §12.1 does **not** apply *to a human*: requiring a human to assemble options would violate §1.3 in the direction the protocol protects. It is not an exemption for everything that is not a session. A creator client is a machine, and one that asks the operator SHOULD carry options and evidence exactly as a session does. A surface cannot tell the two apart — client identity is not a claim about being human — so this is an obligation on authors, not a schema a surface can enforce. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| OWP-L648-MUST-1 | MUST | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc2.md#L648) | **An operator client MUST be able to tell the two directions apart**, and a surface MUST let it: `work.view`'s `open_questions` entries carry `direction`. Routing them correctly into the queue while leaving the item read ambiguous is not enough — a deployment rendered its own operator's question back to that operator as a fork to decide, and since §12.1 excuses humans from assembling options, it arrived as a decision with nothing to decide between `[research]`. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| OWP-L649-MUST-1 | MUST | [12.6 Upstream questions [experimental]](spec/owp-1.0-rc2.md#L649) | **An operator client MUST be able to tell the two directions apart**, and a surface MUST let it: `work.view`'s `open_questions` entries carry `direction`. Routing them correctly into the queue while leaving the item read ambiguous is not enough — a deployment rendered its own operator's question back to that operator as a fork to decide, and since §12.1 excuses humans from assembling options, it arrived as a decision with nothing to decide between `[research]`. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| OWP-L663-MUST-1 | MUST | [13.1 Completion](spec/owp-1.0-rc2.md#L663) | `work.complete` MUST include a valid CompletionRecord. Two planes meet here, and this is the only moment the protocol asks for structure — so it asks cheaply. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L679-MAY-1 | MAY | [13.1 Completion](spec/owp-1.0-rc2.md#L679) | 3. A client with appropriate authority MAY complete straight to exit where the deployment's policy allows it. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L693-MAY-1 | MAY | [13.2 The knowledge plane](spec/owp-1.0-rc2.md#L693) | The protocol defines this **contract** and not a retrieval mechanism. `knowledge.query` is deliberately thin; a surface declares its retrieval capability through `surface.describe().features.knowledge_query` and MAY answer `none`. Retrieval strategy — grep, full-text, embeddings — is the deepest deployment concern there is, and a protocol that specified it would be specifying a search engine. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L756-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L756) | **`policies` carries the standing rules for an act that cannot be undone.** A row MUST carry the applicable policies (§7.6, same set and order as `policy.applicable`) when the action that clears it is irreversible, and the field is absent when none apply. §10 makes an agent consult policy before an irreversible action and hands it the applicable rules in its assignment packet; the operator's own projections handed them nothing, so for the one class of decision an estate exists to make safe, the payload this section calls "sufficient to decide from the row" was missing the rules the operator wrote themselves. The asymmetry runs backwards from §1.3: the machine was briefed and the human — slower, more tired, more likely to be on a phone — was not `[ops]`. `triage`, `answer` and `unpark` are irreversible; `release` is not (the item returns to `todo` and keeps its worklog), and `reconcile` is not the reader's act at all. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L771-MUST-NOT-1 | MUST NOT | [15. Projections](spec/owp-1.0-rc2.md#L771) | **`action` names the act that clears the row — not necessarily an act its reader may perform.** A row whose clearing act belongs to another actor is **informational**: a client MUST NOT offer it as an operator action and SHOULD render it as an observation. `reconcile` is the one such action this document defines — §8 says a session clears drift by writing the worklog forward, so nothing an operator client offers will clear that row. The danger is specific and was shipped: a console that fell through to a `release` branch answered a drift row by yanking the item out of a working agent's hands `[code]`. Clients branch on `action`; `reason` is prose for a person and never a contract. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L772-SHOULD-1 | SHOULD | [15. Projections](spec/owp-1.0-rc2.md#L772) | **`action` names the act that clears the row — not necessarily an act its reader may perform.** A row whose clearing act belongs to another actor is **informational**: a client MUST NOT offer it as an operator action and SHOULD render it as an observation. `reconcile` is the one such action this document defines — §8 says a session clears drift by writing the worklog forward, so nothing an operator client offers will clear that row. The danger is specific and was shipped: a console that fell through to a `release` branch answered a drift row by yanking the item out of a working agent's hands `[code]`. Clients branch on `action`; `reason` is prose for a person and never a contract. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L780-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L780) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L783-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L783) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L784-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L784) | **The order MUST be total, and totality is a property of rows, not of items.** §11 orders the *work*; this orders the *queue*, and one item can raise several rows at one instant — two open questions, two link entries reporting drift. A surface MUST therefore order on something that separates two rows of the same kind, on the same target, at the same age, and MUST carry that discriminator in the cursor. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L796-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L796) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L798-MUST-1 | MUST | [15. Projections](spec/owp-1.0-rc2.md#L798) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L798-MUST-NOT-2 | MUST NOT | [15. Projections](spec/owp-1.0-rc2.md#L798) | **`cursor` MUST be a place, not a position.** It is an **opaque token** identifying the last row returned; a surface resumes *after that row* in the total order. Clients MUST echo it unchanged, and MUST NOT construct it, parse it, or do arithmetic on it. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L852-MUST-1 | MUST | [16. Errors](spec/owp-1.0-rc2.md#L852) | A surface MUST answer failures with these codes. Clients branch on them, so they are protocol, not binding detail. Transport mapping belongs to the binding annex (§17). | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L863-MUST-NOT-1 | MUST NOT | [16. Errors](spec/owp-1.0-rc2.md#L863) | \| `FORBIDDEN` \| authority, grant, or project-boundary violation \| **MUST NOT retry**; audited (§18) \| | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L868-MAY-1 | MAY | [16. Errors](spec/owp-1.0-rc2.md#L868) | A surface MAY add codes; a client MUST treat an unrecognized code as terminal. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L868-MUST-2 | MUST | [16. Errors](spec/owp-1.0-rc2.md#L868) | A surface MAY add codes; a client MUST treat an unrecognized code as terminal. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L872-MUST-NOT-1 | MUST NOT | [16. Errors](spec/owp-1.0-rc2.md#L872) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L875-MUST-1 | MUST | [16. Errors](spec/owp-1.0-rc2.md#L875) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L877-SHOULD-1 | SHOULD | [16. Errors](spec/owp-1.0-rc2.md#L877) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| OWP-L877-MUST-2 | MUST | [16. Errors](spec/owp-1.0-rc2.md#L877) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| OWP-L880-SHOULD-1 | SHOULD | [16. Errors](spec/owp-1.0-rc2.md#L880) | **Transport failure is not one of these codes, and is not itself terminal.** Every code above describes a surface that *answered*. A client that could not reach a surface at all has no answer to relay, and MUST NOT invent a code in this set for it — the rule above would then make the one condition every client should retry the one condition it gives up on. This does **not** authorize a blind retry of a mutation: the client MUST use its binding's replay protection with the same operation key, or reconcile by reading before deciding whether to resubmit. A client SHOULD use bounded exponential backoff with jitter and MUST stop retrying when its local operation deadline or cancellation signal fires. A proxying operator client (§17) that must report the failure in this envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L924-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L924) | **Agent authority**: sessions, work, events, own-claim release, discovery. MUST NOT triage, promote, set or retire policy, cancel work, unpark others' triggers, release others' claims, or delete. **`work.release` is agent authority only for the caller's own claim**; releasing another session's claim requires operator authority or a grant — which is what makes the janitor grant (`{verbs:["work.release"]}`) mean something rather than restate a power the class already had `[code]`. This binds the **class**, not the session: an agent-authority client that registered no session holds no claim of its own, so it needs the grant like anyone else. A surface that tests "is this a different session?" instead of "does this actor own this claim?" leaves the whole estate open to any credential that simply omits a header — which the reference implementation did `[code]`. An agent MAY resolve session identities **within its own project** (§15) — §7.4 requires every client to tell a session id from a client identity, and a session already sees peer ids in `owner_session`, so the resolution leaks nothing and the alternative is a requirement no agent can obey. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L935-MAY-1 | MAY | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L935) | **Agent authority**: sessions, work, events, own-claim release, discovery. MUST NOT triage, promote, set or retire policy, cancel work, unpark others' triggers, release others' claims, or delete. **`work.release` is agent authority only for the caller's own claim**; releasing another session's claim requires operator authority or a grant — which is what makes the janitor grant (`{verbs:["work.release"]}`) mean something rather than restate a power the class already had `[code]`. This binds the **class**, not the session: an agent-authority client that registered no session holds no claim of its own, so it needs the grant like anyone else. A surface that tests "is this a different session?" instead of "does this actor own this claim?" leaves the whole estate open to any credential that simply omits a header — which the reference implementation did `[code]`. An agent MAY resolve session identities **within its own project** (§15) — §7.4 requires every client to tell a session id from a client identity, and a session already sees peer ids in `owner_session`, so the resolution leaks nothing and the alternative is a requirement no agent can obey. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L958-MUST-1 | MUST | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L958) | **Grants compose: union across grants, intersection within one.** A client holding `{work.unpark, "window:"}` and `{work.unpark, "lock:"}` owns both prefixes. A surface that resolves only the first grant naming a verb silently voids the rest. "Intersection within one" is the half that is easy to half-implement: **every field a grant names MUST hold**, on every path the verb can be reached by. A surface that checked `trigger_prefix` and ignored `project` left both things grants exist to bound — a janitor and a cron — unbounded `[code]`. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L964-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L964) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L965-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L965) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L966-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L966) | **No actor may decide its own output.** A grant holder MUST NOT triage an > item whose pending completion record it authored, MUST NOT accept an item > it proposed, and MUST NOT answer or reject a question it asked. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L980-MUST-1 | MUST | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L980) | **Authorship is recorded, never inferred.** A surface enforcing this MUST know who authored the completion record and who proposed the item. Deriving it from event history is guesswork that fails on exactly the items with complicated histories `[desk][research]`. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L985-MUST-NOT-1 | MUST NOT | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L985) | A surface that cannot determine authorship for a given item MUST NOT substitute a guess: the invariant does not apply when the required authorship fact was not recorded. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L988-SHOULD-1 | SHOULD | [18. Security — authority, authentication, audit](spec/owp-1.0-rc2.md#L988) | **Audit.** Surfaces SHOULD log every verb call with its actor, **including denials**. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1002-MAY-1 | MAY | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1002) | **All other names are the deployment's.** Two deployments MAY use the same name for different shapes; they do not interoperate on that name, which is the expected consequence of local vocabulary. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| OWP-L1005-MUST-1 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1005) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1006-MUST-NOT-1 | MUST NOT | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1006) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L1006-MUST-2 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1006) | **Must-preserve, must-ignore.** A surface MUST round-trip envelope values it does not understand and MUST NOT reject them. A client MUST ignore unknown fields and unknown envelope values rather than fail. | Reference CLI/UI and applicable domain clients | owp-code/test/{conformance,fleet,storyboard,console}.test.ts; domain unit/E2E/UI suites; operator acceptance runbooks. | PASS | None. |
| OWP-L1011-MUST-1 | MUST | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1011) | **No-calendar reserved names.** The field names `due`, `deadline`, `due_date`, and `expected_by` are reserved so the §1 no-calendar invariant cannot be reintroduced accidentally as a deployment extension. When one is supplied on a protocol record, a surface MUST either reject the record or ignore the field. It MUST NOT store, return, rank, transition, schedule, or otherwise interpret the value as protocol semantics. This rule is about fields, not open envelope values: deployments remain free to use temporal words inside their own links, triggers, policies, and prose. | Reference surface and bindings; affected deployments | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain HTTP/E2E suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1012-MUST-NOT-1 | MUST NOT | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1012) | **No-calendar reserved names.** The field names `due`, `deadline`, `due_date`, and `expected_by` are reserved so the §1 no-calendar invariant cannot be reintroduced accidentally as a deployment extension. When one is supplied on a protocol record, a surface MUST either reject the record or ignore the field. It MUST NOT store, return, rank, transition, schedule, or otherwise interpret the value as protocol semantics. This rule is about fields, not open envelope values: deployments remain free to use temporal words inside their own links, triggers, policies, and prose. | Reference surface and all deployments using the behavior | owp-code/test/conformance.test.ts; owp-code/conformance/run.ts; schema reproduction; affected domain suites. | PASS | None for the reference implementation; independent implementation evidence is not yet available. |
| OWP-L1017-SHOULD-1 | SHOULD | [19. Extensions and registered names](spec/owp-1.0-rc2.md#L1017) | A deployment publishes its vocabulary through `surface.describe` (§9) and SHOULD document it in prose beside its code. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| OWP-L1040-MUST-1 | MUST | [20. Conformance](spec/owp-1.0-rc2.md#L1040) | **A conforming deployment MUST publish its vocabulary** through §9. | All five runnable repositories | Each README.md, types/registry.md, EVIDENCE.md, docs/deploy.md, clean-room npm test, plugin validation, and standalone start/stop acceptance. | PASS | None. |
| HTTP-L24-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc2.md#L24) | Every verb request MUST carry `Content-Type: application/json` (an optional charset parameter is allowed). A surface MUST reject any other or absent media type with `VALIDATION`; accepting form-compatible types lets an unrelated browser origin drive a loopback surface with ambient authority. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L25-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc2.md#L25) | Every verb request MUST carry `Content-Type: application/json` (an optional charset parameter is allowed). A surface MUST reject any other or absent media type with `VALIDATION`; accepting form-compatible types lets an unrelated browser origin drive a loopback surface with ambient authority. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L28-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc2.md#L28) | A surface MUST reject a verb body larger than 1 MiB with `VALIDATION`, before parsing or dispatch. Deployments MAY impose a smaller documented limit. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L29-MAY-1 | MAY | [A.1 Routes](spec/annex-http-1.0-rc2.md#L29) | A surface MUST reject a verb body larger than 1 MiB with `VALIDATION`, before parsing or dispatch. Deployments MAY impose a smaller documented limit. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L31-MUST-1 | MUST | [A.1 Routes](spec/annex-http-1.0-rc2.md#L31) | A path that is verb-shaped but names a verb the surface does not implement MUST answer `UNKNOWN_VERB` — **not** a transport 404. A binding that conflates "this route did not parse" with "this surface lacks that verb" makes discovery unusable, and clients cannot tell a missing feature from a typo'd URL. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L35-MAY-1 | MAY | [A.1 Routes](spec/annex-http-1.0-rc2.md#L35) | Surfaces MAY serve other paths (an operator UI at `/`, static assets); those are outside this annex. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L46-MAY-1 | MAY | [A.2 Actor identity](spec/annex-http-1.0-rc2.md#L46) | A request MAY carry both a session and a client identity; the session wins for attribution (§7.4). Every verb call refreshes the session's `last_seen`, so heartbeats piggyback. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L57-MAY-1 | MAY | [A.3 Responses](spec/annex-http-1.0-rc2.md#L57) | `result` MAY be `null` — notably `work.next` with nothing eligible, which is a normal answer and MUST NOT be an error. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L58-MUST-NOT-1 | MUST NOT | [A.3 Responses](spec/annex-http-1.0-rc2.md#L58) | `result` MAY be `null` — notably `work.next` with nothing eligible, which is a normal answer and MUST NOT be an error. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L77-MUST-1 | MUST | [A.4 Error mapping](spec/annex-http-1.0-rc2.md#L77) | Clients MUST branch on `error.code`, never on the status alone. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L81-MUST-NOT-1 | MUST NOT | [A.4 Error mapping](spec/annex-http-1.0-rc2.md#L81) | **A failure to reach the surface has no code in this table and no status at all** (§16). A connection refused, a DNS failure, or a timeout is not an answer, and a client MUST NOT map it onto one of the codes above — `UNKNOWN_VERB` for a 404 from a load balancer, or `VALIDATION` for a 400 from a proxy, turns a retryable outage into a terminal error. A proxying operator client that must report the failure in the §16 envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`, and a 502 or 504 from an intermediary belongs in the same class. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L84-SHOULD-1 | SHOULD | [A.4 Error mapping](spec/annex-http-1.0-rc2.md#L84) | **A failure to reach the surface has no code in this table and no status at all** (§16). A connection refused, a DNS failure, or a timeout is not an answer, and a client MUST NOT map it onto one of the codes above — `UNKNOWN_VERB` for a 404 from a load balancer, or `VALIDATION` for a 400 from a proxy, turns a retryable outage into a terminal error. A proxying operator client that must report the failure in the §16 envelope SHOULD use a code outside the set and treat it as retryable; the reference deployments use `SURFACE_UNREACHABLE`, and a 502 or 504 from an intermediary belongs in the same class. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L90-MAY-1 | MAY | [A.5 Revision selection](spec/annex-http-1.0-rc2.md#L90) | A client MAY pin a revision with `x-owp-protocol: 1.0-rc2`. A surface that does not implement the pinned revision MUST answer `UNSUPPORTED_VERSION` with `data.supported`. Absent the header, the surface answers in its default revision, which `surface.describe` reports. Discovery (§9) is the intended handshake: call `surface.describe` first, then pin. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L91-MUST-1 | MUST | [A.5 Revision selection](spec/annex-http-1.0-rc2.md#L91) | A client MAY pin a revision with `x-owp-protocol: 1.0-rc2`. A surface that does not implement the pinned revision MUST answer `UNSUPPORTED_VERSION` with `data.supported`. Absent the header, the surface answers in its default revision, which `surface.describe` reports. Discovery (§9) is the intended handshake: call `surface.describe` first, then pin. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L98-MAY-1 | MAY | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L98) | A client MAY send `Idempotency-Key` on any verb request. The value MUST be 1–128 ASCII characters from `A-Z`, `a-z`, `0-9`, `.`, `_`, `:`, or `-`. | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L98-MUST-2 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L98) | A client MAY send `Idempotency-Key` on any verb request. The value MUST be 1–128 ASCII characters from `A-Z`, `a-z`, `0-9`, `.`, `_`, `:`, or `-`. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L101-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L101) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L102-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L102) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L103-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L103) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L104-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L104) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L106-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L106) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L107-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L107) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L108-MUST-NOT-1 | MUST NOT | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L108) | For a given authenticated actor and key, a surface MUST execute the first request at most once and MUST replay its complete status and response for an identical retry. The mutation and replay record MUST commit atomically. Reusing the key with a different verb or arguments MUST return `VALIDATION` without dispatching the request. Equality is over the parsed verb and canonical JSON arguments, not insignificant object-key order or whitespace. A surface MUST retain replay records for at least 24 hours and SHOULD document longer retention; a client MUST NOT reuse a key within that interval. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L110-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L110) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L111-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L111) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L112-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L112) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L112-MUST-2 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L112) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L114-SHOULD-1 | SHOULD | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L114) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L115-MUST-1 | MUST | [A.6 Replay-safe mutation retries](spec/annex-http-1.0-rc2.md#L115) | A client that retries a mutation after connection failure or timeout MUST send the same key. Without a key, it MUST reconcile with a read before resubmitting. Clients SHOULD use bounded exponential backoff with jitter and MUST obey a local operation deadline or cancellation signal. The protocol does not set one universal request timeout: operators SHOULD configure one appropriate to the deployment, and proxies MUST eventually abort an unresponsive upstream rather than hold connections without bound. | HTTP clients and operator proxies | owp-code/src/cli.ts; all four tools/{loadboard,board,desk,brief}.ts; UI call helpers; no shipped client automatically retries mutations. | PASS | Backoff is non-applicable until a shipped client adds automatic retry; timeout and replay-key behavior are implemented now. |
| HTTP-L121-MAY-1 | MAY | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L121) | Authentication is a binding concern (§18); this annex describes the reference arrangement, which a deployment MAY replace wholesale: | Implementations selecting the optional behavior | Permission recorded in the normative inventory; surface.describe and vocabulary declarations expose optional behavior where required. | N/A (permission) | A MAY grants permission and imposes no implementation obligation by itself. |
| HTTP-L128-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L128) | **Open mode** — no credentials configured. Authority is inferred: session actors are agent-class, bare clients operator-class. Appropriate only where the transport itself is the trust boundary: one omitted header buys operator authority, so the reference surface binds **loopback only** in open mode and exposing it on a routable interface is an explicit opt-out. Loopback alone is not a browser security boundary: implementations MUST also enforce A.1's JSON-only request rule and SHOULD reject unexpected `Origin` headers. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L129-SHOULD-1 | SHOULD | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L129) | **Open mode** — no credentials configured. Authority is inferred: session actors are agent-class, bare clients operator-class. Appropriate only where the transport itself is the trust boundary: one omitted header buys operator authority, so the reference surface binds **loopback only** in open mode and exposing it on a routable interface is an explicit opt-out. Loopback alone is not a browser security boundary: implementations MUST also enforce A.1's JSON-only request rule and SHOULD reject unexpected `Origin` headers. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L136-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L136) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L137-MUST-NOT-1 | MUST NOT | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L137) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L137-SHOULD-2 | SHOULD | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L137) | Bearer credentials are secrets. A token-mode deployment exposed beyond loopback MUST use authenticated TLS (directly or at a trusted reverse proxy), MUST NOT place tokens in URLs or logs, and SHOULD issue separate high-entropy tokens per authority/grant so one can be revoked without rotating every actor. | Routable token-mode deployments | All release acceptance runs use loopback; each docs/deploy.md requires TLS and browser authentication before routable use. | N/A (conditional) | A real reverse-proxy/TLS deployment is outside this local release candidate; the condition remains a deployment acceptance gate. |
| HTTP-L140-MUST-1 | MUST | [A.7 Authentication, browser boundary, and grants](spec/annex-http-1.0-rc2.md#L140) | `surface.describe` MUST report the caller's own authority and grants, so a client never has to learn its permissions by being refused. | HTTP binding and HTTP clients | owp-code/test/binding-revision.test.ts; owp-code/conformance/run.ts (Annex sections); domain proxy E2E tests. | PASS | None for the reference binding; independent implementation evidence is not yet available. |
| HTTP-L146-SHOULD-1 | SHOULD | [A.8 Deprecations carried in 1.0](spec/annex-http-1.0-rc2.md#L146) | Per §4.5, event payloads carry both `actor` (normative) and `session` (the deprecated alias). Clients SHOULD read `actor`; surfaces SHOULD emit both until a revision removes the alias. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
| HTTP-L146-SHOULD-2 | SHOULD | [A.8 Deprecations carried in 1.0](spec/annex-http-1.0-rc2.md#L146) | Per §4.5, event payloads carry both `actor` (normative) and `session` (the deprecated alias). Clients SHOULD read `actor`; surfaces SHOULD emit both until a revision removes the alias. | Specification and schema maintainers | RC2 prose/schema diff review; npm run schema; schema/verify.ts; RELEASE_READINESS.md compatibility assessment. | PASS | None. |
